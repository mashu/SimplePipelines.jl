var documenterSearchIndex = {"docs":
[{"category":"section","location":"doctests/#Doctests","page":"Doctest blocks","text":"Runnable jldoctest blocks checked by Documenter.jl on every full doc build and from Pkg.test() via makedocs(doctest = :only) (see docs/make.jl and the DOCUMENTER_DOCTEST environment variable).","title":"Doctests"},{"category":"section","location":"doctests/#Shell-sequence","page":"Doctest blocks","text":"julia> using SimplePipelines\n\njulia> r = run(sh\"echo hi\" >> sh\"echo second\", verbose=false);\n\njulia> length(r)\n2\n\njulia> all(x -> x.success, r)\ntrue","title":"Shell sequence"},{"category":"section","location":"doctests/#Named-steps-and-parallel","page":"Doctest blocks","text":"julia> using SimplePipelines\n\njulia> a = @step a = sh\"echo A\";\n\njulia> b = @step b = sh\"echo B\";\n\njulia> r = run(a & b, verbose=false);\n\njulia> length(r)\n2\n\njulia> all(x -> x.success, r)\ntrue","title":"Named steps and parallel"},{"category":"section","location":"doctests/#Fallback","page":"Doctest blocks","text":"julia> using SimplePipelines\n\njulia> r = run((@step bad = sh\"false\") | (@step ok = sh\"echo ok\"), verbose=false);\n\njulia> r[end].success\ntrue","title":"Fallback"},{"category":"section","location":"doctests/#resolve-returns-NoWork-when-the-target-already-exists","page":"Doctest blocks","text":"julia> using SimplePipelines\n\njulia> nw = mktempdir() do dir\n           cd(dir) do\n               write(\"done.txt\", \"ok\")\n               r = @rule a([] => \"done.txt\") = \"echo y > {output}\"\n               resolve([r], [\"done.txt\"])\n           end\n       end;\n\njulia> nw isa NoWork\ntrue","title":"resolve returns NoWork when the target already exists"},{"category":"section","location":"development/#Extending-SimplePipelines","page":"Extending & dev workflow","text":"SimplePipelines is designed for easy extension via Julia's multiple dispatch.","title":"Extending SimplePipelines"},{"category":"section","location":"development/#Testing-and-coverage","page":"Extending & dev workflow","text":"Run the test suite:\n\njulia --project=. -e 'using Pkg; Pkg.test()'","title":"Testing and coverage"},{"category":"section","location":"development/#Building-the-docs","page":"Extending & dev workflow","text":"The docs project lives in docs/ (docs/Project.toml). docs/make.jl sets root = dirname(@__FILE__), so you can run it from any working directory.\n\njulia --project=docs -e 'using Pkg; Pkg.develop(path=\"..\"); Pkg.instantiate(); include(\"make.jl\")'\n\nRun from the repository root (so path=\"..\" resolves to the package), or use an absolute path to the package instead of \"..\".","title":"Building the docs"},{"category":"section","location":"development/#Doctests","page":"Extending & dev workflow","text":"Runnable examples live in docs/src/doctests.md as Documenter jldoctest fenced code blocks. They run whenever makedocs runs (see the doctest keyword in docs/make.jl).\n\nFull HTML build plus doctests: default DOCUMENTER_DOCTEST unset or true.\nDoctests only (no HTML; fast): set DOCUMENTER_DOCTEST=only before include(\"make.jl\"). This is what Pkg.test() runs in a subprocess so CI exercises the same checks as the doc build.\nTo skip doctests temporarily: DOCUMENTER_DOCTEST=false.\n\nRun tests with code coverage (for local profiling or CI):\n\njulia --project=. -e 'using Pkg; Pkg.test(coverage=true)'\n\nCoverage is reported in lcov.info; CI uploads it to Codecov.","title":"Doctests"},{"category":"section","location":"development/#Architecture","page":"Extending & dev workflow","text":"All nodes inherit from AbstractNode. To add custom behavior, define a new subtype and implement dispatch methods:\n\nAbstractNode\n    ├── Step{F}           # Leaf node\n    ├── Sequence{T}       # Sequential\n    ├── Parallel{T}       # Concurrent\n    ├── Retry{N}          # Retry on failure\n    ├── Fallback{A,B}     # Try A, else B\n    └── Branch{C,T,F}     # Conditional","title":"Architecture"},{"category":"section","location":"development/#Custom-Node-in-4-Steps","page":"Extending & dev workflow","text":"using SimplePipelines\nimport SimplePipelines: AbstractNode, run_node, print_dag, count_steps, steps\n\n# 1. Define type (parametric for type stability)\nstruct Timeout{N<:AbstractNode} <: AbstractNode\n    node::N\n    seconds::Float64\nend\n\n# 2. Implement execution (signature: v, forced::Bool, context_input=nothing)\nfunction run_node(t::Timeout, v, forced::Bool, context_input=nothing)\n    # ... timeout logic ...\n    return run_node(t.node, v, forced, context_input)\nend\n\n# 3. Implement visualization (signature: io, node, pre, cont, color)\nfunction print_dag(io::IO, t::Timeout, pre::String, cont::String, color::Bool)\n    print(io, pre, \"⏱ Timeout \", t.seconds, \"s\\n\")\n    print_dag(io, t.node, cont * \"    \", cont * \"    \", color)\nend\n\n# 4. Implement utilities\ncount_steps(t::Timeout) = count_steps(t.node)\nsteps(t::Timeout) = steps(t.node)","title":"Custom Node in 4 Steps"},{"category":"section","location":"development/#Custom-Operator","page":"Extending & dev workflow","text":"import Base: ^\n\n# Node^3 means repeat 3 times\n^(node::AbstractNode, n::Int) = Repeat(node, n)","title":"Custom Operator"},{"category":"section","location":"development/#Custom-Step-Executor","page":"Extending & dev workflow","text":"Extend execute for new work types:\n\nstruct HTTPGet\n    url::String\nend\n\nfunction SimplePipelines.execute(step::Step{HTTPGet})\n    start = time()\n    resp = HTTP.get(step.work.url)\n    return StepResult(step, resp.status == 200, time() - start, step.inputs, step.outputs, String(resp.body))\nend\n\n# Usage: Step(:fetch, HTTPGet(\"https://api.example.com\"))","title":"Custom Step Executor"},{"category":"section","location":"development/#Type-Stability-Rules","page":"Extending & dev workflow","text":"Use parametric types: struct MyNode{T<:AbstractNode} not children::Vector{AbstractNode}\nUse tuple recursion for heterogeneous collections\nDispatch on types, don't check with isa","title":"Type Stability Rules"},{"category":"section","location":"examples/bioinformatics/#Examples:-bioinformatics","page":"Bioinformatics","text":"Longer domain workflows. Tool names are real; runs require installed software and inputs unless you substitute echo placeholders (see examples/bioinformatics.jl in the repo).\n\nMore examples: Basics · Control flow · Complex DAGs\n\nPattern: Multiple donors/samples — use ForEach with {wildcard} patterns so files are discovered and each match becomes a parallel branch.","title":"Examples: bioinformatics"},{"category":"section","location":"examples/bioinformatics/#4.1-Immune-repertoire-(single-donor)","page":"Bioinformatics","text":"Flow: Paired-end FASTQ → merge (PEAR) → FASTQ→FASTA → IgBLAST → Julia filter by identity.\n\n  R1.fastq ──┐\n             ├──►  pear  ──►  to_fasta  ──►  igblast  ──►  filter_identity  ──►  igblast_filtered.tsv\n  R2.fastq ──┘\n\nusing SimplePipelines\nusing DelimitedFiles\n\npear = @step pear = sh\"pear -f R1.fastq -r R2.fastq -o merged\"\nto_fasta = @step to_fasta = sh\"seqtk seq -A merged.assembled.fastq > merged.assembled.fasta\"\nigblast = @step igblast = sh\"igblastn -query merged.assembled.fasta -germline_db_V V.fasta -germline_db_D D.fasta -germline_db_J J.fasta -outfmt 7 -out igblast.tsv\"\n\nfilter_identity = @step filter_identity = () -> begin\n    data, header_row = readdlm(\"igblast.tsv\", '\\t', header=true)\n    header = vec(header_row)\n    v_idx = findfirst(isequal(\"v_identity\"), header)\n    j_idx = findfirst(isequal(\"j_identity\"), header)\n    to_float(x) = something(tryparse(Float64, string(x)), 0.0)\n    filtered = [r for r in eachrow(data) if to_float(r[v_idx]) > 90.0 && to_float(r[j_idx]) > 90.0]\n    writedlm(\"igblast_filtered.tsv\", vcat(header', filtered), '\\t')\n    return \"Filtered $(length(filtered)) sequences\"\nend\n\npipeline = pear >> to_fasta >> igblast >> filter_identity\nrun(Pipeline(pipeline, name=\"Immune Repertoire\"))","title":"4.1 Immune repertoire (single donor)"},{"category":"section","location":"examples/bioinformatics/#4.2-Immune-repertoire-(multiple-donors)","page":"Bioinformatics","text":"Flow: ForEach discovers fastq/{donor}_R1.fq.gz → one branch per donor.\n\n  ForEach(\"fastq/{donor}_R1.fq.gz\")\n       │\n       ├── donor1: pear → fasta → igblast  ──┐\n       ├── donor2: ...                      ──┼── (parallel)\n       └── donorN: ...                      ──┘\n\nusing SimplePipelines\n\npipeline = ForEach(\"fastq/{donor}_R1.fq.gz\") do donor\n    sh(\"pear -f fastq/$(donor)_R1.fq.gz -r fastq/$(donor)_R2.fq.gz -o $(donor)_merged\") >>\n    sh(\"seqtk seq -A $(donor)_merged.assembled.fastq > $(donor).fasta\") >>\n    sh(\"igblastn -query $(donor).fasta -germline_db_V V.fasta -germline_db_D D.fasta -germline_db_J J.fasta -outfmt 7 -out $(donor)_igblast.tsv\")\nend\n\nrun(pipeline)","title":"4.2 Immune repertoire (multiple donors)"},{"category":"section","location":"examples/bioinformatics/#4.3-Variant-calling","page":"Bioinformatics","text":"Flow: QC → trim → align → sort/index → call → index VCF → filter.\n\n  R1.fq.gz ──┐\n             ├──►  fastqc  ──►  trim  ──►  align  ──►  index  ──►  call  ──►  index_vcf  ──►  filter_vcf\n  R2.fq.gz ──┘\n\nusing SimplePipelines\n\nfastqc = @step fastqc = sh\"fastqc -o qc/ R1.fq.gz R2.fq.gz\"\ntrim = @step trim = sh\"trimmomatic PE R1.fq.gz R2.fq.gz R1_trimmed.fq.gz R1_unpaired.fq.gz R2_trimmed.fq.gz R2_unpaired.fq.gz ILLUMINACLIP:adapters.fa:2:30:10 LEADING:3 TRAILING:3 SLIDINGWINDOW:4:15 MINLEN:36\"\n\nalign = @step align = sh\"bwa mem -t 8 GRCh38.fa R1_trimmed.fq.gz R2_trimmed.fq.gz | samtools sort -@ 4 -o aligned.bam -\"\nindex = @step index = sh\"samtools index aligned.bam\"\n\ncall = @step call = sh\"bcftools mpileup -f GRCh38.fa aligned.bam | bcftools call -mv -Oz -o variants.vcf.gz\"\nindex_vcf = @step index_vcf = sh\"bcftools index variants.vcf.gz\"\nfilter_vcf = @step filter_vcf = sh\"bcftools filter -i 'QUAL>=20' variants.vcf.gz -Oz -o filtered.vcf.gz\"\n\npipeline = fastqc >> trim >> align >> index >> call >> index_vcf >> filter_vcf\nrun(Pipeline(pipeline, name=\"Variant Calling\"))","title":"4.3 Variant calling"},{"category":"section","location":"guide/control-flow/#Control-flow","page":"Control flow","text":"Optional behaviors around failure, choice, and time limits.","title":"Control flow"},{"category":"section","location":"guide/control-flow/#Fallback:","page":"Control flow","text":"If the primary fails, run the fallback:\n\nfast_method = @step fast = sh\"fast_tool input.txt\"\nslow_method = @step slow = sh\"slow_tool input.txt\"\npipeline = fast_method | slow_method\n\nmethod_a = @step a = sh\"method_a input\"\nmethod_b = @step b = sh\"method_b input\"\nmethod_c = @step c = sh\"method_c input\"\npipeline = method_a | method_b | method_c","title":"Fallback: |"},{"category":"section","location":"guide/control-flow/#Retry:-or-Retry()","page":"Control flow","text":"Retry a node up to N times on failure:\n\nflaky_api_call = @step api = sh\"echo 'mock response'\"\npipeline = flaky_api_call^3\n\nnetwork_request = @step fetch = sh\"echo 'data'\"\npipeline = Retry(network_request, 5, delay=2.0)\n\nprimary = @step primary = sh\"echo primary\"\nfallback = @step fallback = sh\"echo fallback\"\npipeline = primary^3 | fallback","title":"Retry: ^ or Retry()"},{"category":"section","location":"guide/control-flow/#Branch-(conditional)","page":"Control flow","text":"Pick a branch using a runtime condition:\n\nlarge_file_pipeline = @step large = sh\"process_large data.txt\"\nsmall_file_pipeline = @step small = sh\"process_small data.txt\"\npipeline = Branch(\n    () -> filesize(\"data.txt\") > 1_000_000,\n    large_file_pipeline,\n    small_file_pipeline,\n)\n\ndebug_steps = @step debug = sh\"run with verbose logging\"\nnormal_steps = @step normal = sh\"run quietly\"\npipeline = Branch(\n    () -> haskey(ENV, \"DEBUG\"),\n    debug_steps,\n    normal_steps,\n)","title":"Branch (conditional)"},{"category":"section","location":"guide/control-flow/#Timeout","page":"Control flow","text":"Fail if a node exceeds a time limit:\n\nlong_running_step = @step long = sh\"sleep 1\"\npipeline = Timeout(long_running_step, 30.0)\n\napi_call = @step api = sh\"echo result\"\nbackup = @step backup = sh\"echo fallback\"\npipeline = Timeout(api_call, 5.0)^3 | backup\n\nNext: Fan-out and reduce — ForEach and Reduce.","title":"Timeout"},{"category":"section","location":"api/#API-Reference","page":"API","text":"","title":"API Reference"},{"category":"section","location":"api/#Module","page":"API","text":"","title":"Module"},{"category":"section","location":"api/#Types","page":"API","text":"","title":"Types"},{"category":"section","location":"api/#Macros","page":"API","text":"","title":"Macros"},{"category":"section","location":"api/#Resource-aware-execution","page":"API","text":"Annotate heavy nodes with with_resources and pass memory_budget_mb / thread_budget to run so the parallel scheduler keeps total concurrent resource use under a soft cap. Wrap heavy values in FilePath and use materialize at the consumer to keep memory bounded between steps.","title":"Resource-aware execution"},{"category":"section","location":"api/#Output-side-wildcard-inference","page":"API","text":"Declare reusable Rules with {wildcard} patterns, then call resolve to walk a target list backward through the rule set and build a runnable DAG. Rule work supports both shell templates and (inputs, outputs, wildcards) -> Cmd | String | Function.\n\nexpand generates concrete target lists from a template by Cartesian product, and a Workflow bundles rules + default targets behind a single run(::Workflow) entry point.","title":"Output-side wildcard inference"},{"category":"section","location":"api/#Operators","page":"API","text":"The package extends these operators for pipeline composition. Cmd and Function arguments are auto-wrapped in Step.\n\nOperator Name Description\n>> Sequence Run in order; pass previous output to next (function step)\n& Parallel Run nodes concurrently\n| Fallback Run fallback if primary fails\n^ Retry Wrap with retries, e.g. node^3\n|> Pipe Run right with left's output(s) (single or vector)\n>>> SameInputPipe Run both with the same input (e.g. branch id)\n.>> BroadcastPipe Attach right to each branch of left (per-branch pipeline)\n\nWhen the left has one output, >>, |>, and .>> all pass that value to the next step. When the left has multiple outputs (ForEach, Parallel):\n\nLeft side  Pipe \nSingle output step(one value) step(one value) step(one value)\nMulti output step(last only) step(vector of all) step per branch (one call each)","title":"Operators"},{"category":"section","location":"api/#Functions","page":"API","text":"","title":"Functions"},{"category":"section","location":"api/#Shell","page":"API","text":"The string macro shell_raw\"...\" (and triple-quoted shell_raw\\\"\\\"\\\"...\\\"\\\"\\\") is documented in @shell_raw_str; use it for scripts where the dollar sign must not be interpreted by Julia.\n\nsh_pipe(cmd1, cmd2, ...) folds several Cmds into one OS-level pipeline, so stdout flows through OS pipes without Julia-side buffering between stages. The @step macro recognises sh_pipe and evaluates it eagerly, so the resulting Step's work field is a Base.AbstractCmd rather than a thunk.","title":"Shell"},{"category":"section","location":"api/#Execution","page":"API","text":"Execution is recursive: run(pipeline) calls run_node(root, ...) which dispatches on node type and recurses (Sequence in order, Parallel/ForEach with optional @spawn).","title":"Execution"},{"category":"section","location":"api/#Defaults","page":"API","text":"These are used as the default keyword argument values in run.","title":"Defaults"},{"category":"section","location":"api/#Freshness-and-state","page":"API","text":"State is stored in .pipeline_state as a fixed-layout, memory-mapped file. Completions are batched and written when run() finishes.","title":"Freshness and state"},{"category":"section","location":"api/#State-file-format","page":"API","text":"The state file uses a fixed binary layout (see src/StateFormat.jl) for random access and mmap.","title":"State file format"},{"category":"section","location":"api/#Utilities","page":"API","text":"","title":"Utilities"},{"category":"section","location":"api/#Index","page":"API","text":"","title":"Index"},{"category":"type","location":"api/#SimplePipelines.AbstractNode","page":"API","text":"AbstractNode\n\nAbstract supertype of all pipeline nodes (Step, Sequence, Parallel, Retry, Fallback, Branch, Timeout, Force, Reduce, ForEach, Pipe, SameInputPipe, BroadcastPipe). Constructors only build the struct; execution is via run, which builds a RunContext and dispatches to run_node(node, ctx, forced, context_input).\n\n\n\n\n\n","title":"SimplePipelines.AbstractNode"},{"category":"type","location":"api/#SimplePipelines.AbstractStepResult","page":"API","text":"Supertype of all step results; use for Vector{AbstractStepResult} (e.g. return of run).\n\n\n\n\n\n","title":"SimplePipelines.AbstractStepResult"},{"category":"type","location":"api/#SimplePipelines.StepResult","page":"API","text":"StepResult(step, success, duration, inputs, outputs, result)\n\nResult of running one step. Type is StepResult{S, I, O, V}. Type-stable: no Any.\n\nFields (all real)\n\ninputs — Input file paths the step declared. Empty for steps that take no input files (e.g. download / start nodes).\noutputs — Output file paths the step declared (files the step produces).\nresult — Execution result: stdout for shell steps, return value for function steps. Wrap large values in FilePath so the in-memory result stays small.\n\n\n\n\n\n","title":"SimplePipelines.StepResult"},{"category":"type","location":"api/#SimplePipelines.Step","page":"API","text":"Step{F} <: AbstractNode\n\nA single unit of work in a pipeline. F is the work type: Cmd (backtick or sh\"...\"), Function, or ShRun (from sh(f)).\n\nFields\n\nname::Symbol — Step identifier (auto-generated if not provided)\nwork::F — The command or function to execute\ninputs::Vector{String} — Input file dependencies\noutputs::Vector{String} — Output file paths\n\nConstructors\n\nStep(work)                           # Auto-generated name\nStep(name::Symbol, work)             # Named step\nStep(name, work, inputs, outputs)    # Full specification\n@step name = work                    # Macro form\n@step name(inputs => outputs) = work # Macro with dependencies\n\nSee also: @step, is_fresh, Force\n\n\n\n\n\n","title":"SimplePipelines.Step"},{"category":"type","location":"api/#SimplePipelines.ShRun","page":"API","text":"ShRun{F}\n\nInternal callable wrapper around a thunk that returns a shell command string at execution time. Use sh(cmd_func) in steps; calling sr() invokes sr.f() and returns the command string. The pipeline prints the command when verbose=true.\n\n\n\n\n\n","title":"SimplePipelines.ShRun"},{"category":"type","location":"api/#SimplePipelines.Sequence","page":"API","text":"Sequence <: AbstractNode\na >> b\n\nExecutes nodes sequentially, stopping on first failure. Data passing: the next node receives one value: the previous step's output (declared output paths when applicable, else the step's result). When the previous node produced multiple results (e.g. Parallel/ForEach), the next node receives only the last branch's output. Distinct from |> (which passes a vector of all) and .>> (which runs the next step once per branch).\n\nChildren are stored in a Vector{AbstractNode} so Sequence scales to thousands of children without compile-time tuple-type blow-up.\n\n\n\n\n\n","title":"SimplePipelines.Sequence"},{"category":"type","location":"api/#SimplePipelines.Parallel","page":"API","text":"Parallel <: AbstractNode\n\nExecutes nodes concurrently using threads. Created automatically by the & operator. Children are stored in a Vector{AbstractNode} so wide fan-outs (hundreds of samples) do not blow up compile time.\n\n\n\n\n\n","title":"SimplePipelines.Parallel"},{"category":"type","location":"api/#SimplePipelines.Retry","page":"API","text":"Retry{N} <: AbstractNode\n\nRetries a node up to max_attempts times on failure, with optional delay. Created by the ^ operator or Retry() constructor.\n\n\n\n\n\n","title":"SimplePipelines.Retry"},{"category":"type","location":"api/#SimplePipelines.Fallback","page":"API","text":"Fallback{A,B} <: AbstractNode\n\nExecutes fallback node if primary fails. Created by the | operator.\n\n\n\n\n\n","title":"SimplePipelines.Fallback"},{"category":"type","location":"api/#SimplePipelines.Branch","page":"API","text":"Branch{C,T,F} <: AbstractNode\n\nConditional execution based on a predicate function.\n\n\n\n\n\n","title":"SimplePipelines.Branch"},{"category":"type","location":"api/#SimplePipelines.Timeout","page":"API","text":"Timeout{N} <: AbstractNode\n\nWraps a node with a time limit. Returns failure if time exceeded.\n\n\n\n\n\n","title":"SimplePipelines.Timeout"},{"category":"type","location":"api/#SimplePipelines.Reduce","page":"API","text":"Reduce{F,N} <: AbstractNode\n\nRuns a parallel node and combines successful step outputs with a reducer function.\n\n\n\n\n\n","title":"SimplePipelines.Reduce"},{"category":"type","location":"api/#SimplePipelines.Force","page":"API","text":"Force{N} <: AbstractNode\n\nForces execution of a node, bypassing freshness checks. See also is_fresh, clear_state!.\n\n\n\n\n\n","title":"SimplePipelines.Force"},{"category":"type","location":"api/#SimplePipelines.Pipeline","page":"API","text":"Pipeline{N<:AbstractNode}\n\nA named pipeline wrapping a root node for execution.\n\n\n\n\n\n","title":"SimplePipelines.Pipeline"},{"category":"type","location":"api/#SimplePipelines.Pipe","page":"API","text":"Pipe{A, B} <: AbstractNode\na |> b\n\nRun a, then run b with a's output(s) as b's input. RHS must be a function step. Same notion of output as >> (declared output paths when applicable, else result). Single result → one value; multiple results (ForEach/Parallel) → vector of all branch outputs. Distinct from >> (which passes only the last) and .>> (which runs the next step once per branch).\n\n\n\n\n\n","title":"SimplePipelines.Pipe"},{"category":"type","location":"api/#SimplePipelines.SameInputPipe","page":"API","text":"SameInputPipe{A, B} <: AbstractNode\na >>> b\n\nRun a then b; both receive the same context input. b does not receive a's output. Distinct from >> and |> (where b receives a's output). Use when the next step should run on the same input (e.g. branch id) as the previous.\n\n\n\n\n\n","title":"SimplePipelines.SameInputPipe"},{"category":"type","location":"api/#SimplePipelines.BroadcastPipe","page":"API","text":"BroadcastPipe{A, B} <: AbstractNode\na .>> b   (broadcast of >>)\n\nRun the next step once per branch of the left node, each with that branch's output. For ForEach or Parallel, branches run in parallel. For a single-output node, equivalent to a >> b. Distinct from >> (one run, last branch only) and |> (one run with vector of all outputs).\n\n\n\n\n\n","title":"SimplePipelines.BroadcastPipe"},{"category":"type","location":"api/#SimplePipelines.Resources","page":"API","text":"Resources(; mem_mb=0, threads=1)\n\nResource hint for a node. mem_mb is the peak in-memory footprint (megabytes) expected while the node runs; threads is the number of CPU threads it intends to use. A 0 field means \"do not charge against the corresponding budget\".\n\n\n\n\n\n","title":"SimplePipelines.Resources"},{"category":"type","location":"api/#SimplePipelines.Resourced","page":"API","text":"Resourced{N} <: AbstractNode\n\nWraps a node with Resources. The runtime acquires the matching budget slots on the RunContext before executing the wrapped node, releasing them on completion.\n\n\n\n\n\n","title":"SimplePipelines.Resourced"},{"category":"type","location":"api/#SimplePipelines.FilePath","page":"API","text":"FilePath(path::String)\n\nWrapper marking that a step's value lives on a user-named file at path. Steps that write their output to a known path should return FilePath(...) so the runtime holds only a path between steps. The default materialize reads raw bytes; users can specialise for typed loading (CSV → DataFrame, etc.).\n\n\n\n\n\n","title":"SimplePipelines.FilePath"},{"category":"type","location":"api/#SimplePipelines.SpilledValue","page":"API","text":"SpilledValue(path::String)\n\nWrapper marking that a step's value was auto-spilled to a runtime-managed tempfile at path. Created by the runtime when auto_spill is on and a step's result exceeds spill_threshold_bytes. materialize(::SpilledValue) deserialises the value via Base.Serialization.\n\nDistinct from FilePath: FilePath is for user-named output files (arbitrary contents, raw-bytes default loader), SpilledValue is for serialised Julia objects the runtime owns.\n\n\n\n\n\n","title":"SimplePipelines.SpilledValue"},{"category":"type","location":"api/#SimplePipelines.Rule","page":"API","text":"Rule(name, inputs, outputs, work)\n\nA reusable production recipe. inputs and outputs are vectors of pattern strings (e.g. \"data/{sample}.fq\"). work is either:\n\nString: a shell command template using {input}, {output}, {wildcard} placeholders.\nFunction(inputs, outputs, wildcards): builds a node (Step / Cmd / function) at resolve time.\n\nUse @rule for ergonomic construction, and resolve to turn a set of rules + targets into a runnable DAG.\n\n\n\n\n\n","title":"SimplePipelines.Rule"},{"category":"type","location":"api/#SimplePipelines.NoWork","page":"API","text":"NoWork() <: AbstractNode\n\nSentinel node that performs no work and produces no step results. Returned by resolve when every target is already on disk.\n\n\n\n\n\n","title":"SimplePipelines.NoWork"},{"category":"macro","location":"api/#SimplePipelines.@step","page":"API","text":"@step name = work\n@step name(inputs => outputs) = work\n@step work\n\nCreate a named step with optional file dependencies. Steps are lazy: if the right-hand side is a function call (other than sh(...)), it is wrapped in a thunk and runs only when the pipeline is run via run(pipeline).\n\nUse sh\"...\" for literal commands; use sh(\"... \" * var * \" ...\") when you need interpolation at construction time. For commands built at run time, use sh(cmd_func) where cmd_func returns the command string; with verbose=true, the command is printed before execution. For shell scripts that use shell variables, use shell_raw\"...\" or shell_raw\"\"\"...\"\"\" (multiline) so Julia does not interpret the dollar sign.\n\nExamples\n\n@step download = sh\"curl -o data.csv http://example.com\"\n@step download([] => [\"data.csv\"]) = sh(\"curl -L -o \" * repr(path) * \" \" * url)\n@step process([\"input.csv\"] => [\"output.csv\"]) = sh\"sort input.csv > output.csv\"\n@step call_tool([ref, bams] => [out]) = sh(() -> \"bcftools mpileup -f \" * repr(ref) * \" \" * join(repr.(bams), \" \"))\n@step process(\"path\") = process_file   # function by name, receives path at run time\n@step sh\"echo hello\"\n\n\n\n\n\n","title":"SimplePipelines.@step"},{"category":"macro","location":"api/#SimplePipelines.@rule","page":"API","text":"@rule name(inputs => outputs) = work\n\nConstruct a Rule. Mirrors @step but uses patterns rather than concrete paths.\n\nExamples\n\nalign = @rule align(\"data/{sample}.fq\" => \"out/{sample}.bam\") =\n    \"bwa mem ref.fa {input} > {output}\"\nindex = @rule index(\"out/{sample}.bam\" => \"out/{sample}.bam.bai\") =\n    \"samtools index {input}\"\n\nplan = resolve([align, index], [\"out/A.bam.bai\", \"out/B.bam.bai\"])\nrun(plan)\n\n\n\n\n\n","title":"SimplePipelines.@rule"},{"category":"macro","location":"api/#SimplePipelines.@sh_str","page":"API","text":"sh\"command\"\nsh(command::String)\n\nShell commands: use sh\"...\" for literals, sh(\"...\") when you need interpolation.\n\nExamples\n\nsh\"sort data.txt > sorted.txt\"\nsh(\"process \\$(sample)_R1.fq\")\n\n\n\n\n\n","title":"SimplePipelines.@sh_str"},{"category":"macro","location":"api/#SimplePipelines.@shell_raw_str","page":"API","text":"shell_raw\"command\"\nshell_raw\"\"\"...\"\"\"\n\nString literal for shell commands where the dollar sign is not interpreted by Julia. Use for scripts that use shell variables. Triple-quoted form keeps multiline scripts readable; shell variables (e.g. $VAR, var) stay intact.\n\nExample (multiline with loop and Julia interpolation)\n\ndonors = [\"A\", \"B\"]\ncmd = shell_raw\"\"\"\nfor d in \"\"\" * join(donors, \" \") * shell_raw\"\"\"\n  do\n    echo \"Processing $d\"\n  done\n\"\"\"\n# Use in a step: sh(() -> cmd)\n\n\n\n\n\n","title":"SimplePipelines.@shell_raw_str"},{"category":"function","location":"api/#SimplePipelines.with_resources","page":"API","text":"with_resources(node; mem_mb=0, threads=1) -> Resourced\nwith_resources(cmd; ...) -> Resourced  (lifts Cmd via Step)\nwith_resources(f::Function; ...) -> Resourced  (lifts Function via Step)\n\nAnnotate a node with resource hints used by the parallel scheduler.\n\n\n\n\n\n","title":"SimplePipelines.with_resources"},{"category":"function","location":"api/#SimplePipelines.materialize","page":"API","text":"materialize(value)\n\nIdentity for plain values. For FilePath, reads raw bytes from disk (extend for typed loaders). For SpilledValue, deserialises the value back into the original Julia object.\n\n\n\n\n\n","title":"SimplePipelines.materialize"},{"category":"function","location":"api/#SimplePipelines.resolve","page":"API","text":"resolve(rules, targets) -> AbstractNode\n\nBuild a DAG that produces every path in targets, working backward through rules. A target that already exists on disk is treated as satisfied (no node emitted for it). Multiple targets sharing a dependency reuse the same Step instance, so the runtime DAG protocol executes that dependency exactly once.\n\nIf every requested target already exists on disk, returns a no-op node (NoWork) that produces no step results when run — making \"rerun the pipeline; everything's fresh\" friendly instead of an error.\n\nThrows if a non-existent target has no producing rule, or if rule resolution forms a cycle.\n\n\n\n\n\n","title":"SimplePipelines.resolve"},{"category":"function","location":"api/#SimplePipelines.expand","page":"API","text":"expand(template, wildcards) -> Vector{String}\nexpand(template; wildcard1=values1, wildcard2=values2, ...) -> Vector{String}\nexpand(templates::AbstractVector, ...) -> Vector{String}\n\nGenerate concrete paths from one or more pattern templates by taking the Cartesian product of the wildcard value lists. wildcards may be a NamedTuple or supplied as keyword arguments; values are anything that can be turned into a string via string.\n\nExamples\n\nexpand(\"out/{sample}.bam\"; sample=[\"A\",\"B\",\"C\"])\n# [\"out/A.bam\", \"out/B.bam\", \"out/C.bam\"]\n\nexpand(\"out/{s}.{ext}\"; s=[\"A\",\"B\"], ext=[\"bam\",\"bai\"])\n# [\"out/A.bam\", \"out/A.bai\", \"out/B.bam\", \"out/B.bai\"]\n\nexpand([\"raw/{s}.fq\", \"qc/{s}.html\"]; s=[\"A\",\"B\"])\n# [\"raw/A.fq\", \"raw/B.fq\", \"qc/A.html\", \"qc/B.html\"]\n\nWhen there are multiple wildcards, Iterators.product is used so combinations are generated with the first keyword varying slowest and the last varying fastest.\n\n\n\n\n\n","title":"SimplePipelines.expand"},{"category":"type","location":"api/#SimplePipelines.Workflow","page":"API","text":"Workflow(; name=\"workflow\") -> Workflow\n\nA registry of Rules and default targets. Sugar over resolve(rules, targets) that lets a user describe a pipeline as one object:\n\nwf = Workflow(name=\"rnaseq\")\npush!(wf,\n    @rule align(\"raw/{s}.fq\" => \"out/{s}.bam\") = \"bwa mem ref.fa {input} > {output}\",\n    @rule index(\"out/{s}.bam\" => \"out/{s}.bam.bai\") = \"samtools index {input}\")\npush!(wf, expand(\"out/{s}.bam.bai\"; s=[\"A\",\"B\",\"C\"]))\nrun(wf)\n\nItems pushed to a workflow are dispatched by type: Rule items go to wf.rules, strings (or vectors of strings) go to wf.targets. You can override targets at run time: run(wf; targets=[\"out/A.bam.bai\"]).\n\n\n\n\n\n","title":"SimplePipelines.Workflow"},{"category":"function","location":"api/#SimplePipelines.plan","page":"API","text":"plan(wf::Workflow; targets=wf.targets) -> AbstractNode\n\nResolve the workflow's rules against targets, returning a node ready to run. Useful for inspecting the build plan with print_dag before execution.\n\n\n\n\n\n","title":"SimplePipelines.plan"},{"category":"type","location":"api/#SimplePipelines.ForEach","page":"API","text":"Lazy node: run block over file matches (pattern string) or over a collection (vector). Dispatches on second argument.\n\n\n\n\n\n","title":"SimplePipelines.ForEach"},{"category":"type","location":"api/#SimplePipelines.fe","page":"API","text":"Short alias for ForEach. Use fe(\"pattern\") do x ... end.\n\n\n\n\n\n","title":"SimplePipelines.fe"},{"category":"function","location":"api/#SimplePipelines.sh","page":"API","text":"Shell command with string (build-time). For run-time command use sh(cmd_func). See also @sh_str.\n\n\n\n\n\n","title":"SimplePipelines.sh"},{"category":"function","location":"api/#SimplePipelines.sh_pipe","page":"API","text":"sh_pipe(cmds::Base.AbstractCmd...) -> Base.AbstractCmd\n\nCompose two or more shell commands into one OS-level pipeline. The resulting AbstractCmd runs all stages as a single Base.run, with stdout flowing through OS pipes between stages — no Julia-side buffering between commands. Only the final stage's stdout is captured by the runtime.\n\nThis is an alias for Base.pipeline; the package recognises it inside @step so the call is not thunked.\n\nExample\n\n@step align([\"foo.bam\"] => [\"filtered.txt\"]) =\n    sh_pipe(sh\"samtools view foo.bam\", sh\"awk -F'\\t' '$5>30'\", sh\"sort > filtered.txt\")\n\nCompare with the materialising >> form, where each step captures its full stdout into Julia memory before the next step runs.\n\nSee also: @step, @sh_str.\n\n\n\n\n\n","title":"SimplePipelines.sh_pipe"},{"category":"function","location":"api/#Base.run","page":"API","text":"run(p::Pipeline; verbose=true, dry_run=false, force=false,\n    jobs=default_jobs(), memory_budget_mb=default_memory_budget_mb(),\n    thread_budget=0,\n    auto_spill=true, spill_threshold_bytes=default_spill_threshold_bytes(),\n    spill_dir=tempdir()) -> Vector{AbstractStepResult}\nrun(node::AbstractNode; kwargs...) -> Vector{AbstractStepResult}\n\nExecute a pipeline or node, returning a StepResult for every step that ran.\n\nKeywords\n\nverbose=true: Show colored progress output.\ndry_run=false: Show DAG structure without executing.\nforce=false: Run all steps regardless of freshness.\njobs: Max concurrent branches for Parallel/ForEach. Defaults to min(Threads.nthreads(), 8) so a fan-out with N threads cannot oversubscribe the machine. Pass jobs=0 to disable (unbounded — only do this if every branch is annotated with with_resources).\nmemory_budget_mb: Soft cap (MB) on concurrent memory across nodes wrapped with with_resources. Defaults to 50% of total system RAM. Branches with declared mem_mb block on a semaphore until enough budget is free. Pass 0 to disable.\nthread_budget=0: Soft cap on concurrent CPU threads across nodes that declare threads; 0 disables the cap.\nauto_spill=true: After a successful step, if Base.summarysize(r.result) > spill_threshold_bytes, serialise it to a tempfile in spill_dir and replace r.result with a SpilledValue. Keeps the per-run memo's RAM footprint bounded regardless of step count. Pass auto_spill=false to keep every result in RAM.\nspill_threshold_bytes: Spill threshold; defaults to 10 MB.\nspill_dir: Where the spill tempfiles live; defaults to tempdir(). Pass a mktempdir() if you want auto-cleanup at scope exit.\n\nMemory safety\n\nThe package treats disk as effectively infinite, RAM as finite. Three defaults combine to make a default-run pipeline memory-safe by construction:\n\njobs caps the live-execution set at the host's thread count.\nauto_spill swaps each finished step's big result for a tiny SpilledValue so the memo doesn't accumulate raw values.\nmemory_budget_mb serialises declared-heavy steps via with_resources.\n\nDownstream consumers call materialize to retrieve a SpilledValue or FilePath. Small step results (below threshold) stay in RAM with no I/O cost.\n\nThe returned vector contains every step's result. If you only want the terminal value, take last(results); if you want successes, filter(r -> r.success, results).\n\nSee also: is_fresh, Force, print_dag, with_resources, FilePath, SpilledValue, materialize, default_jobs, default_memory_budget_mb.\n\n\n\n\n\n","title":"Base.run"},{"category":"function","location":"api/#SimplePipelines.default_jobs","page":"API","text":"default_jobs() -> Int\n\nSafe default for the number of concurrent parallel/foreach branches: at most as many as Threads.nthreads(), capped at 8. Spawning more branches than threads just queues work without speeding it up, but it does multiply memory pressure.\n\n\n\n\n\n","title":"SimplePipelines.default_jobs"},{"category":"function","location":"api/#SimplePipelines.default_memory_budget_mb","page":"API","text":"default_memory_budget_mb() -> Int\n\nSafe default soft cap for the per-run memory budget: 50% of total system RAM, in MB. Steps wrapped with with_resources charge the budget; un-annotated steps don't, so this is only an upper bound on declared concurrent memory. To disable the cap entirely, pass memory_budget_mb=0 to run.\n\nOverride at the user level by passing memory_budget_mb=N to run(...).\n\n\n\n\n\n","title":"SimplePipelines.default_memory_budget_mb"},{"category":"function","location":"api/#SimplePipelines.default_spill_threshold_bytes","page":"API","text":"default_spill_threshold_bytes() -> Int\n\nDefault size above which a step's in-memory result is auto-spilled to disk. 10 MB — small enough to spill anything DataFrame-sized, large enough to keep small dictionaries / counters / arrays in RAM where the I/O cost would dominate.\n\n\n\n\n\n","title":"SimplePipelines.default_spill_threshold_bytes"},{"category":"function","location":"api/#SimplePipelines.is_fresh","page":"API","text":"is_fresh(step::Step) -> Bool\nis_fresh(step::Step, ctx::RunContext) -> Bool\n\nCheck if a step can be skipped based on Make-like freshness rules.\n\nFreshness Rules\n\nHas inputs and outputs: Fresh if all outputs exist and are newer than all inputs.\nHas only outputs: Fresh if outputs exist and step was previously completed.\nNo file dependencies: Fresh if step was previously completed (state-based tracking).\n\nState is persisted in .pipeline_state. Completions during a run are batched and written once when run() finishes.\n\nSee also: Force, clear_state!\n\n\n\n\n\n","title":"SimplePipelines.is_fresh"},{"category":"function","location":"api/#SimplePipelines.clear_state!","page":"API","text":"clear_state!()\n\nRemove the pipeline state file (.pipeline_state), forcing all steps to run on the next execution regardless of freshness.\n\nSee also: is_fresh, Force\n\n\n\n\n\n","title":"SimplePipelines.clear_state!"},{"category":"module","location":"api/#SimplePipelines.StateFormat","page":"API","text":"StateFormat\n\nFixed binary layout for the pipeline state file. Single source of truth for offsets, sizes, and all I/O: memory-mapped file with random access to the hash array.\n\nLayout (on disk):   [0..8)   magic (8 bytes)   [8..16)  count (UInt64, number of valid hashes)   [16..)   hashes (max_hashes × sizeof(UInt64), mmap-able for random access)\n\nAll read/write/mmap of the state file goes through this module (stateinit, stateread, statewrite, stateappend).\n\n\n\n\n\n","title":"SimplePipelines.StateFormat"},{"category":"type","location":"api/#SimplePipelines.StateFormat.StateFileLayout","page":"API","text":"StateFileLayout\n\nDescriptor for the fixed binary layout of the state file. Holds magic bytes, header length, offsets for count and hashes, and max capacity.\n\n\n\n\n\n","title":"SimplePipelines.StateFormat.StateFileLayout"},{"category":"function","location":"api/#SimplePipelines.StateFormat.state_init","page":"API","text":"Ensure the state file exists and has the correct size; create or truncate if needed.\n\n\n\n\n\n","title":"SimplePipelines.StateFormat.state_init"},{"category":"function","location":"api/#SimplePipelines.StateFormat.state_read","page":"API","text":"Read completed step hashes from the state file (memory-mapped, random access). Returns Set{UInt64}.\n\n\n\n\n\n","title":"SimplePipelines.StateFormat.state_read"},{"category":"function","location":"api/#SimplePipelines.StateFormat.state_write","page":"API","text":"Write all completed hashes to the state file (capped at layout.max_hashes).\n\n\n\n\n\n","title":"SimplePipelines.StateFormat.state_write"},{"category":"function","location":"api/#SimplePipelines.StateFormat.state_append","page":"API","text":"Append one hash; returns false if at capacity (caller should statewrite(union(stateread(), [h]))).\n\n\n\n\n\n","title":"SimplePipelines.StateFormat.state_append"},{"category":"function","location":"api/#SimplePipelines.count_steps","page":"API","text":"count_steps(node) -> Int\n\nNumber of distinct leaf steps that may execute. Shared sub-DAGs are counted once. For Branch, returns the maximum of the two branches (only one runs); for Reduce, includes the synthetic reducer step.\n\n\n\n\n\n","title":"SimplePipelines.count_steps"},{"category":"function","location":"api/#SimplePipelines.steps","page":"API","text":"steps(node) -> Vector{Step}\n\nReturn all leaf steps in the DAG (flattened). Shared sub-graphs are listed once.\n\n\n\n\n\n","title":"SimplePipelines.steps"},{"category":"function","location":"api/#SimplePipelines.print_dag","page":"API","text":"print_dag(node [; color=true])\nprint_dag(io, node [, indent])\n\nPrint a tree visualization of the pipeline DAG. With color=true (default when writing to a terminal), uses colors for node types and status. Steps with no inputs (start nodes) are shown with ◆ in light cyan; steps with inputs use ○ in cyan. See also run and display(pipeline).\n\n\n\n\n\n","title":"SimplePipelines.print_dag"},{"category":"section","location":"examples/complex-dags/#Examples:-complex-DAGs","page":"Complex DAGs","text":"Multi-stage fork–join and a pipeline that combines several features.\n\nMore examples: Basics · Control flow · Bioinformatics","title":"Examples: complex DAGs"},{"category":"section","location":"examples/complex-dags/#3.1-Multi-stage-parallel","page":"Complex DAGs","text":"Flow: Two parallel fetch+transform branches, then merge, analyze, then report and archive in parallel.\n\n  ┌── fetch_db   ──►  transform_db  ──┐\n  │                                    ├──►  merge  ──►  analyze  ──►  ┌── report\n  └── fetch_files ──►  transform_files ─┘                               └── archive\n\nGoal: Runnable without network; shows layered & and >>.\n\nusing SimplePipelines\n\nfetch_db = @step db = sh\"echo '{\\\"db\\\":1}' > db_data.json\"\nfetch_files = @step files = sh\"echo 'local_data' > local_data.txt\"\n\ntransform_db = @step transform_db = sh\"wc -c db_data.json > db_size.txt\"\ntransform_files = @step transform_files = sh\"wc -c local_data.txt > files_size.txt\"\n\nmerge = @step merge = sh\"cat db_size.txt files_size.txt > merged.txt\"\nanalyze = @step analyze = sh\"wc -l merged.txt > results.txt\"\n\nreport = @step report = sh\"cat results.txt\"\narchive = @step archive = sh\"gzip -c merged.txt > results.tar.gz\"\n\ndb_branch = fetch_db >> transform_db\nfiles_branch = fetch_files >> transform_files\npipeline = (db_branch & files_branch) >> merge >> analyze >> (report & archive)\n\nrun(pipeline)","title":"3.1 Multi-stage parallel"},{"category":"section","location":"examples/complex-dags/#3.2-Robust-pipeline-(combined-features)","page":"Complex DAGs","text":"Flow: Retry+fallback fetch → conditional process → parallel report and notify (with retry on notify).\n\n  [primary^3 | backup]  ──►  [small? quick : full]  ──►  report\n                                                          └── notify^2\n\nGoal: Illustrate retries, fallback, branching, and parallel tail in one graph.\n\nusing SimplePipelines\n\nprimary_source = @step primary = sh\"echo '{\\\"status\\\":\\\"ok\\\"}' > data.json\"\nbackup_source = @step backup = sh\"echo '{\\\"status\\\":\\\"fallback\\\"}' > data.json\"\nfetch = Retry(primary_source, 3, delay=0.1) | backup_source\n\nquick_process = @step quick = sh\"wc -c data.json > output.txt\"\nfull_process = @step parse = sh\"wc -l data.json > output.txt\" >> @step validate = sh\"wc -c output.txt >> output.txt\"\n\nprocess = Branch(\n    () -> filesize(\"data.json\") < 1_000_000,\n    quick_process,\n    full_process,\n)\n\nreport = @step report = sh\"cat output.txt\"\nnotify = @step notify = sh\"echo 'Pipeline done'\"\n\npipeline = fetch >> process >> (report & Retry(notify, 2))\nrun(Pipeline(pipeline, name=\"Robust ETL\"))","title":"3.2 Robust pipeline (combined features)"},{"category":"section","location":"design/#Design","page":"Internals","text":"","title":"Design"},{"category":"section","location":"design/#Interface-Overview","page":"Internals","text":"┌─────────────────────────────────────────────────────────────┐\n│                    SimplePipelines.jl                       │\n├─────────────────────────────────────────────────────────────┤\n│                                                             │\n│  @step name = sh\"cmd\"       Shell step                      │\n│  @step name = sh\"cmd > f\"   Shell with redirection/pipes    │\n│  @step name = () -> ...     Julia step                      │\n│                                                             │\n│  a >> b                     Sequential; pass output to next (function step) │\n│  a & b                      Parallel: a and b together      │\n│  a |> b                     Pipe: run b with a's output(s)  │\n│  a >>> b                    Same input: both get same context │\n│  a .>> b                    Broadcast: attach b to each branch of a │\n│                                                             │\n│  run(p)                     Execute the pipeline            │\n│                                                             │\n└─────────────────────────────────────────────────────────────┘","title":"Interface Overview"},{"category":"section","location":"design/#Type-Hierarchy","page":"Internals","text":"AbstractNode\n    │\n    ├── Step{F}           Single unit of work\n    │                     F = Cmd | Function\n    │\n    ├── Sequence{T}       Sequential execution\n    │                     T = Tuple of nodes\n    │\n    └── Parallel{T}       Concurrent execution\n                          T = Tuple of nodes\n\nAll types are fully parametric—the compiler knows exact types at every level.","title":"Type Hierarchy"},{"category":"section","location":"design/#Composition-Model","page":"Internals","text":"# User writes:\nsh\"echo a\" >> (sh\"echo b\" & sh\"echo c\") >> sh\"echo d\"\n\n# Becomes:\nSequence{Tuple{\n    Step{Cmd},\n    Parallel{Tuple{Step{Cmd}, Step{Cmd}}},\n    Step{Cmd}\n}}\n\nThe complete structure is encoded in the type, enabling full compile-time specialization.","title":"Composition Model"},{"category":"section","location":"design/#Execution-Flow","page":"Internals","text":"Execution is recursive: dispatch on node type and recurse.\n\nrun(Pipeline)\n       │\n       ▼\nrun_node(root, v, force)  ─── dispatch on node type\n       │\n       ├─► Step:     execute(step) → StepResult\n       ├─► Sequence: run_node each in order; break on first failure\n       ├─► Parallel: @spawn run_node each; fetch and concat\n       ├─► ForEach:  (String) find file matches, or (Vector) iterate items; get nodes from block (cycle check), then run like Parallel\n       └─► Retry/Fallback/Branch/Timeout/Force/Reduce: recurse on inner node(s)\n       │\n       ▼\nVector{AbstractStepResult}","title":"Execution Flow"},{"category":"section","location":"design/#Key-Design-Decisions","page":"Internals","text":"","title":"Key Design Decisions"},{"category":"section","location":"design/#1.-Tuples,-Not-Vectors","page":"Internals","text":"# ✗ Vector: type information lost\nSequence(nodes::Vector{AbstractNode})\n\n# ✓ Tuple: exact types preserved\nSequence{Tuple{Step{Cmd}, Step{Function}}}\n\nTuples enable the compiler to generate specialized code for each node.","title":"1. Tuples, Not Vectors"},{"category":"section","location":"design/#2.-Multiple-Dispatch,-Not-Type-Checks","page":"Internals","text":"# ✗ Runtime type checking (type unstable)\nfunction run_node(node)\n    if node isa Step\n        # ...\n    elseif node isa Sequence\n        # ...\n    end\nend\n\n# ✓ Multiple dispatch (type stable)\nrun_node(step::Step, v) = execute(step)\nrun_node(seq::Sequence, v) = _run_sequence!([], seq.nodes, v)\nrun_node(par::Parallel, v) = _spawn_parallel(par.nodes, v)","title":"2. Multiple Dispatch, Not Type Checks"},{"category":"section","location":"design/#3.-Tuple-Recursion","page":"Internals","text":"Iterate tuples in a type-stable way:\n\n# Base case\n_run_sequence!(results, ::Tuple{}, v) = nothing\n\n# Recursive case\nfunction _run_sequence!(results, nodes::Tuple, v)\n    append!(results, run_node(first(nodes), v))\n    _run_sequence!(results, Base.tail(nodes), v)\nend\n\nThe compiler unrolls this into efficient, specialized code.","title":"3. Tuple Recursion"},{"category":"section","location":"design/#4.-Verbosity-as-Types","page":"Internals","text":"struct Verbose end\nstruct Silent end\n\nlog_start(::Silent, ::Step) = nothing\nlog_start(::Verbose, s::Step) = println(\"▶ \", step_label(s))\n\nDead code elimination removes printing when verbose=false.","title":"4. Verbosity as Types"},{"category":"section","location":"design/#Performance-Characteristics","page":"Internals","text":"Aspect Design Choice Benefit\nNode storage Tuples Full type info, inline storage\nDispatch Multiple dispatch Zero runtime type checks\nIteration Recursion Compiler unrolling\nOperators @inline Zero call overhead\nVerbosity Singleton types Dead code elimination","title":"Performance Characteristics"},{"category":"section","location":"reference/quickref/#Quick-reference","page":"Quick reference","text":"One-page lookup for syntax and APIs. Narrative explanations live in the user guide.","title":"Quick reference"},{"category":"section","location":"reference/quickref/#Steps","page":"Quick reference","text":"Syntax Meaning\n@step name = sh\"cmd\" Shell command\n@step name = sh(\"\\$(var)\") Shell with Julia interpolation\n@step name = sh(cmd_func) Run-time command, e.g. sh(() -> \"tool \" * ref)\nshell_raw\"...\", shell_raw\"\"\"...\"\"\" Literal shell (\\$VAR not expanded by Julia)\n@step name = () -> expr Julia function step","title":"Steps"},{"category":"section","location":"reference/quickref/#Operators","page":"Quick reference","text":"Op Meaning Example\n>> Sequence; passes output into the next function step a >> b >> c\n& Parallel a & b & c\n| Fallback if primary fails a | b\n^n Retry up to n times a^3\n|> Pipe outputs into RHS a |> b\n>>> Same input to both sides a >>> b\n.>> Broadcast: attach RHS to each branch fe .>> step","title":"Operators"},{"category":"section","location":"reference/quickref/#Sequencing-and-piping","page":"Quick reference","text":"When the left side has multiple outputs (ForEach, Parallel), >>, |>, and .>> differ:\n\n| Left | >> | |> | .>> | |:––-|:––-|:––-|:–––| | Single | one value → next | one value | one value | | Multi | last only | vector of all | per branch |","title":"Sequencing and piping"},{"category":"section","location":"reference/quickref/#Control-flow-helpers","page":"Quick reference","text":"API Role\nTimeout(a, secs) Fail if overrun\nBranch(cond, a, b) Conditional subgraph\nForEach(xs) do ... end Fan-out over collection\nForEach(\"pat/{x}.ext\") do ... end Fan-out from glob pattern\nReduce(f, a & b) Merge parallel outputs\nRetry(a, n; delay=d) Retries with optional delay","title":"Control-flow helpers"},{"category":"section","location":"reference/quickref/#Execution","page":"Quick reference","text":"Call / field Role\nrun(p) / p |> run Execute; returns result vector\nrun(..., verbose=false) Quiet (no command echo)\nrun(..., dry_run=true) Preview only\n.success, .duration, .result, .inputs, .outputs Per-step outcome fields","title":"Execution"},{"category":"section","location":"reference/quickref/#Utilities","page":"Quick reference","text":"API Role\nprint_dag(node) Print structure\ncount_steps(node) Step count\nsteps(node) Flat list of steps","title":"Utilities"},{"category":"section","location":"reference/quickref/#Full-detail","page":"Quick reference","text":"Narrative walkthroughs: User guide and Examples. Generated docstrings: API.","title":"Full detail"},{"category":"section","location":"guide/running-and-results/#Running-and-inspecting","page":"Running and inspecting","text":"","title":"Running and inspecting"},{"category":"section","location":"guide/running-and-results/#Running-pipelines","page":"Running and inspecting","text":"Use run(pipeline) or pipeline |> run:\n\nresults = run(pipeline)\n\n# Default: verbose=true prints each shell command before it runs\nresults = run(pipeline)\n\nresults = run(pipeline, verbose=false)\n\nrun(pipeline, dry_run=true)\n\nstep_a = @step a = sh\"first\"\nstep_b = @step b = sh\"second\"\np = Pipeline(step_a >> step_b, name=\"My Workflow\")\nrun(p)","title":"Running pipelines"},{"category":"section","location":"guide/running-and-results/#Checking-results","page":"Running and inspecting","text":"results = run(pipeline)\n\nfor r in results\n    if r.success\n        println(\"$(r.step.name): completed in $(r.duration)s\")\n    else\n        println(\"$(r.step.name): FAILED - $(r.result)\")\n    end\nend\n\nall_ok = all(r -> r.success, results)\n\nRelevant fields include .success, .duration, .result, .inputs, and .outputs.","title":"Checking results"},{"category":"section","location":"guide/running-and-results/#Memory-safety:-auto-spill","page":"Running and inspecting","text":"The package treats disk as effectively infinite, RAM as finite. Three defaults make a default-run pipeline memory-safe by construction:\n\njobs = min(Threads.nthreads(), 8) — concurrent fan-out can't oversubscribe the host.\nauto_spill = true — after a step finishes, if Base.summarysize(r.result) > spill_threshold_bytes (10 MB by default), the value is serialised to a tempfile in spill_dir (defaults to tempdir()) and r.result is replaced with a SpilledValue. Small results stay in RAM (no I/O cost).\nmemory_budget_mb = 50% of total RAM — caps the concurrent memory of nodes wrapped in with_resources.\n\nDownstream consumers call materialize to load a SpilledValue (round-trips via Base.Serialization) or a FilePath (raw bytes by default; users specialise for typed loading like CSV → DataFrame).\n\n# Tight RAM:\nrun(plan; spill_threshold_bytes=1_000_000)        # 1 MB\n\n# Pure in-memory (small data, benchmarks):\nrun(plan; auto_spill=false)\n\n# Self-cleaning spill dir:\nmktempdir() do dir\n    results = run(plan; spill_dir=dir)\n    df = materialize(last(results).result)         # spilled → DataFrame\n    # …work with df…\nend                                                # spill files gone here\n\nA step that already returns a FilePath is left alone — the runtime only spills unwrapped big values.","title":"Memory safety: auto-spill"},{"category":"section","location":"guide/running-and-results/#Mixing-shell-and-Julia","page":"Running and inspecting","text":"Shell commands and Julia functions compose seamlessly:\n\nprep = @step prep = () -> begin\n    raw = read(\"raw.csv\", String)\n    cleaned = filter(line -> !isempty(strip(line)), split(raw, '\\n'))\n    write(\"clean.csv\", join(cleaned, '\\n'))\n    return \"Wrote $(length(cleaned)) lines\"\nend\n\nexternal = @step tool = sh\"wc -l clean.csv > result.txt\"\n\npost = @step post = () -> begin\n    n = parse(Int, split(read(\"result.txt\", String))[1])\n    return \"Line count: $n\"\nend\n\npipeline = prep >> external >> post\nrun(pipeline)","title":"Mixing shell and Julia"},{"category":"section","location":"guide/running-and-results/#Utilities","page":"Running and inspecting","text":"n = count_steps(pipeline)\nall_steps = steps(pipeline)\nprint_dag(pipeline)","title":"Utilities"},{"category":"section","location":"guide/running-and-results/#Where-to-go-next","page":"Running and inspecting","text":"Examples — runnable patterns in order of difficulty.\nQuick reference — one-page operator and API tables.\nAPI — full docstrings.","title":"Where to go next"},{"category":"section","location":"guide/steps-and-shell/#Steps-and-shell","page":"Steps and shell","text":"A step is a shell command or a Julia function. Optionally attach file inputs/outputs to a step.","title":"Steps and shell"},{"category":"section","location":"guide/steps-and-shell/#Shell-commands","page":"Steps and shell","text":"using SimplePipelines\n\n# Direct command (anonymous step)\nstep = @step sh\"samtools sort input.bam\"\n\n# Named step\nstep = @step sort = sh\"samtools sort input.bam\"\n\n# Shell features (>, |, &&) — use sh\"...\"\nstep = @step sort = sh\"sort data.txt | uniq > sorted.txt\"\n\n# Shell variables (\\$VAR) — use shell_raw so Julia does not interpolate\ndonors = [\"A\", \"B\"]\nstep = @step process = sh(() -> shell_raw\"for d in \" * join(donors, \" \") * shell_raw\"; do echo \\$d; done\")\n# Multiline: shell_raw\"\"\"...\"\"\" for readability","title":"Shell commands"},{"category":"section","location":"guide/steps-and-shell/#Julia-functions","page":"Steps and shell","text":"# Anonymous function step\nstep = @step () -> process_data()\n\n# Named function step\nstep = @step analyze = () -> run_analysis(\"data.csv\")","title":"Julia functions"},{"category":"section","location":"guide/steps-and-shell/#File-dependencies-(optional)","page":"Steps and shell","text":"Track input/output files for validation:\n\n@step align(\"reads.fq\" => \"aligned.bam\") = sh\"bwa mem ref.fa reads.fq > aligned.bam\"\n\nNext: Composing pipelines — chaining steps with >>, |>, parallelism, and DAG shapes.","title":"File dependencies (optional)"},{"category":"section","location":"examples/control-flow/#Examples:-control-flow","page":"Control flow","text":"Retry, fallback, and conditional branches.\n\nMore examples: Basics · Complex DAGs · Bioinformatics","title":"Examples: control flow"},{"category":"section","location":"examples/control-flow/#2.1-Retry-and-fallback","page":"Control flow","text":"Flow (retry): Run a step up to N times, then continue.\n\n  [fetch^3]  ──►  process\n\nFlow (fallback): If primary fails, run fallback.\n\n   primary  ──►  (success) result\n      │\n      └──►  fallback ──►  result\n\nGoal: Retry a flaky fetch; fall back when the primary fails; combine both.\n\nusing SimplePipelines\n\nfetch = @step fetch = sh\"echo '{\\\"x\\\":1}' > data.json\"\nprocess = @step process = sh\"wc -c data.json\"\npipeline = Retry(fetch, 3, delay=0.1) >> process\n\nrun(@step setup = sh\"(echo 'a,b'; echo '1,2') > data.csv\", verbose=false)\nfast = @step fast = sh\"sort data.csv > sorted.csv\"\nslow = @step slow = sh\"cat data.csv > sorted.csv\"\npipeline = fast | slow\n\npipeline = Retry(fast, 3) | slow\nrun(pipeline)","title":"2.1 Retry and fallback"},{"category":"section","location":"examples/control-flow/#2.2-Conditional-branching","page":"Control flow","text":"Flow: One of two branches runs based on a condition.\n\n            ┌── if true  ──►  branch_a\n  condition ─┤\n            └── if false ──►  branch_b\n\nGoal: Choose processing by file size or environment.\n\nusing SimplePipelines\n\nrun(@step setup = sh\"(echo 'a,b'; echo '1,2'; echo '3,4') > data.csv\", verbose=false)\n\nsmall_pipeline = @step small = sh\"head -n 1000 data.csv > sample.csv\"\nlarge_pipeline = @step decompress = sh\"gunzip -c data.csv.gz > data.csv\" >> @step process = sh\"split -l 10000 data.csv chunk_\"\npipeline = Branch(\n    () -> filesize(\"data.csv\") < 100_000_000,\n    small_pipeline,\n    large_pipeline,\n)\nrun(pipeline)\n\ndebug_steps = @step debug = sh\"echo 'debug mode'\"\nprod_steps = @step prod = sh\"echo 'production'\"\npipeline = Branch(() -> get(ENV, \"DEBUG\", \"0\") == \"1\", debug_steps, prod_steps)\nrun(pipeline)","title":"2.2 Conditional branching"},{"category":"section","location":"guide/foreach-reduce/#Fan-out-and-reduce","page":"Fan-out and reduce","text":"","title":"Fan-out and reduce"},{"category":"section","location":"guide/foreach-reduce/#ForEach-(pattern-or-collection)","page":"Fan-out and reduce","text":"Two modes:\n\nCollection — apply the block to each item (like map).\nPattern — discover files by wildcard pattern and run the block per match.\n\nsamples = [\"sample_A\", \"sample_B\", \"sample_C\"]\nmerge_results = @step merge_results = sh\"echo merge done\"\npipeline = ForEach(samples) do s\n    Step(Symbol(\"process_\", s), sh(\"analyze $s.fastq\"))\nend >> merge_results\n\nPattern-based discovery scans the filesystem; matching files must exist.\n\ncd(mktempdir()) do\n    write(\"s1.fastq\", \"\"); write(\"s2.fastq\", \"\")\n    mkpath(\"fastq\"); write(\"fastq/s1_R1.fq.gz\", \"\"); write(\"fastq/s2_R1.fq.gz\", \"\")\n    mkpath(\"data/p1\"); write(\"data/p1/s1.csv\", \"\")\n\n    ForEach(\"{sample}.fastq\") do sample\n        sh(\"process $(sample).fastq\")\n    end\n\n    ForEach(\"fastq/{sample}_R1.fq.gz\") do sample\n        sh(\"pear $(sample)_R1 $(sample)_R2\") >> sh(\"analyze $(sample)\")\n    end\n\n    ForEach(\"data/{project}/{sample}.csv\") do project, sample\n        sh(\"process $(project)/$(sample).csv\")\n    end\n\n    ForEach(\"{id}.fastq\") do id\n        sh(\"align $(id).fastq\")\n    end >> @step merge = sh\"merge *.bam\"\nend","title":"ForEach (pattern or collection)"},{"category":"section","location":"guide/foreach-reduce/#Reduce-(combine-parallel-outputs)","page":"Fan-out and reduce","text":"Collect outputs from parallel steps and combine them:\n\nanalyze_a = @step a = sh\"echo result_a\"\nanalyze_b = @step b = sh\"echo result_b\"\npipeline = Reduce(analyze_a & analyze_b) do outputs\n    join(outputs, \"\\n\")\nend\n\ncombine_results(outputs) = join(outputs, \"\\n\")\nstep_a = @step a = sh\"echo result_a\"\nstep_b = @step b = sh\"echo result_b\"\nstep_c = @step c = sh\"echo result_c\"\npipeline = Reduce(combine_results, step_a & step_b & step_c)\n\nmerge_outputs(outputs) = join(outputs, \"\\n\")\nfetch = @step fetch = sh\"echo data\"\nanalyze_a = @step a = sh\"wc -c\"\nanalyze_b = @step b = sh\"wc -l\"\nreport = @step report = sh\"echo done\"\npipeline = fetch >> Reduce(merge_outputs, analyze_a & analyze_b) >> report\n\nThe reducer receives a vector of outputs from successful parallel branches (element types depend on upstream steps).","title":"Reduce (combine parallel outputs)"},{"category":"section","location":"guide/foreach-reduce/#Low-memory-/-large-data","page":"Fan-out and reduce","text":"The runtime keeps every step's result alive in the per-run memo (so DAG sharing works correctly). To keep memory bounded:\n\nHave each step write its result to a file and return a FilePath — only the path travels between steps; the underlying value is freed.\nLet the reducer open or stream paths one at a time and return a combined path.\nThe returned vector contains every step's result; if you only need the final value, take last(results).\n\nNext: Running and inspecting — run, results, utilities, shell + Julia together.","title":"Low-memory / large data"},{"category":"section","location":"examples/basics/#Examples:-basics","page":"Basics","text":"Sequential pipelines, parallelism, Julia + shell, and data flow (>>, |>, .>>).\n\nMore examples: Control flow · Complex DAGs · Bioinformatics","title":"Examples: basics"},{"category":"section","location":"examples/basics/#1.1-Basic-pipeline","page":"Basics","text":"Flow: Three steps in sequence.\n\n  download  ──►  process  ──►  upload\n\nGoal: Create a file, process it, then copy the result (runnable without network).\n\nusing SimplePipelines\n\ndownload = @step download = sh\"(echo line3; echo line1; echo line2) > data.txt\"\nprocess = @step process = sh\"sort data.txt > sorted.txt\"\nupload = @step upload = sh\"cp sorted.txt uploaded.txt\"\n\npipeline = download >> process >> upload\nrun(pipeline)","title":"1.1 Basic pipeline"},{"category":"section","location":"examples/basics/#1.2-Parallel-processing","page":"Basics","text":"Flow: Three steps run in parallel, then one step merges.\n\n       ┌── file_a ──┐\n       ├── file_b ──┼──►  archive\n       └── file_c ──┘\n\nGoal: Create three files, compress them concurrently, then archive.\n\nusing SimplePipelines\n\nfile_a = @step a = sh\"echo content_a > file_a.txt && gzip -k file_a.txt\"\nfile_b = @step b = sh\"echo content_b > file_b.txt && gzip -k file_b.txt\"\nfile_c = @step c = sh\"echo content_c > file_c.txt && gzip -k file_c.txt\"\narchive = @step archive = sh\"tar -cvf archive.tar file_a.txt.gz file_b.txt.gz file_c.txt.gz\"\n\npipeline = (file_a & file_b & file_c) >> archive\nrun(pipeline)","title":"1.2 Parallel processing"},{"category":"section","location":"examples/basics/#1.3-Julia-shell","page":"Basics","text":"Flow: Julia step → shell step → Julia step.\n\n  generate  ──►  process  ──►  report\n   (Julia)       (shell)      (Julia)\n\nGoal: Generate data in Julia, run a shell tool, then summarize in Julia.\n\nusing SimplePipelines\nusing DelimitedFiles\n\ngenerate = @step generate = () -> begin\n    data = rand(100, 10)\n    writedlm(\"matrix.csv\", data, ',')\n    return \"Generated $(size(data)) matrix\"\nend\n\nprocess = @step process = sh\"wc -l matrix.csv\"\n\nreport = @step report = () -> begin\n    lines = read(\"matrix.csv\", String)\n    nrows = count(==('\\n'), lines)\n    return \"Matrix has $nrows rows\"\nend\n\npipeline = generate >> process >> report\nrun(pipeline)","title":"1.3 Julia + shell"},{"category":"section","location":"examples/basics/#1.4-Data-passing-and-pipe","page":"Basics","text":"Flow: Function steps receive the previous step's output; |> pipes output forward; .>> attaches the next step to each branch.\n\n  download(id)  ──►  process(path)     # >> passes path\n  ForEach .>> step                     # each branch: branch >> step\n\nGoal: Pass output between function steps; use broadcast for per-branch follow-up.\n\nusing SimplePipelines\n\ndownload() = \"data.csv\"\nprocess(path) = path * \"_done\"\npipeline = @step dl = download >> @step proc = process\nrun(pipeline)\n\nfetch = @step fetch = `echo \"hello\"`\npipeline = fetch |> @step process = (s -> \"got: \" * strip(String(s)))\nrun(pipeline)\n\nfe = ForEach([1, 2]) do x\n    Step(Symbol(\"gen_\", x), `echo $x`)\nend\npipeline = fe .>> @step process = (s -> \"got_\" * strip(String(s)))\nrun(pipeline, force=true)","title":"1.4 Data passing and pipe"},{"category":"section","location":"guide/composition/#Composing-pipelines","page":"Composing pipelines","text":"Build workflows by combining steps with operators. Start with >> (sequence) and & (parallel), then use pipe variants when you need finer control over how values flow between branches.","title":"Composing pipelines"},{"category":"section","location":"guide/composition/#Sequential-execution:","page":"Composing pipelines","text":"The >> operator chains steps: each waits for the previous to complete. When the next node is a function step, it receives the previous step's output (or the current branch context inside ForEach):\n\n# Chain commands directly (anonymous steps)\npipeline = sh\"download data.txt\" >> sh\"process data.txt\" >> sh\"upload results.txt\"\n\n# Data passing: function steps receive previous output\ndownload(id) = \"data_$(id).csv\"\nprocess(path) = read(path, String)\npipeline = @step dl = download >> @step proc = process\n\n# Named steps\nstep_a = @step step_a = sh\"download data.txt\"\nstep_b = @step step_b = sh\"process data.txt\"\nstep_c = @step step_c = sh\"upload results.txt\"\npipeline = step_a >> step_b >> step_c","title":"Sequential execution: >>"},{"category":"section","location":"guide/composition/#Pipe-(),-same-input-(),-and-broadcast-(.)","page":"Composing pipelines","text":"When the left has one output, >>, |>, and .>> all pass that value to the next (function) step. When the left has multiple outputs (e.g. ForEach, Parallel), they differ:\n\n| Left side     | >>                | |>                     | .>>                      | |:–––––––|:––––––––––|:––––––––––––-|:–––––––––––––-| | Single output | step(one value)     | step(one value)          | step(one value)            | | Multi output  | step(last only) | step(vector of all)  | step per branch        |\n\na |> b — Run a, then run b with a's output(s). RHS must be a function step. Multi-branch → one call with a vector.\na >>> b — Run a then b with the same input (e.g. inside ForEach, both get the branch id).\na .>> b — Attach b to each branch of a. Each branch runs as branch >> b; you do not wait for all of a to finish before starting b on completed branches.\n\nfetch = @step fetch = `echo \"content\"`\nprocess(x) = uppercase(String(x))\npipeline = fetch |> @step process = process\n\nForEach([1, 2]) do id\n    @step first = (x -> \"a_$(x)\") >>> @step second = (x -> \"b_$(x)\")\nend\n\nForEach([\"a\", \"b\"]) do x\n    Step(Symbol(\"echo_\", x), `echo $x`)\nend .>> @step process = (s -> \"got_\" * strip(String(s)))\n\nSee also the quick reference tables for a compact summary.","title":"Pipe (|>), same input (>>>), and broadcast (.>>)"},{"category":"section","location":"guide/composition/#Parallel-execution:-and","page":"Composing pipelines","text":"The & operator groups steps to run concurrently:\n\nparallel = sh\"task_a\" & sh\"task_b\" & sh\"task_c\"\n\nsample_1 = @step s1 = sh\"process sample1\"\nsample_2 = @step s2 = sh\"process sample2\"\nsample_3 = @step s3 = sh\"process sample3\"\nmerge_results = @step merge = sh\"merge outputs\"\npipeline = (sample_1 & sample_2 & sample_3) >> merge_results","title":"Parallel execution: &"},{"category":"section","location":"guide/composition/#Complex-DAGs","page":"Composing pipelines","text":"Combine >> and & for fork–join graphs.","title":"Complex DAGs"},{"category":"section","location":"guide/composition/#Diamond-pattern","page":"Composing pipelines","text":"       ┌── analyze_a ──┐\n fetch─┤               ├── report\n       └── analyze_b ──┘\n\nfetch = @step fetch = sh\"echo 'a,b\\n1,2' > data.csv\"\nanalyze_a = @step a = sh\"wc -l data.csv\"\nanalyze_b = @step b = sh\"wc -c data.csv\"\nreport = @step report = () -> \"done\"\n\npipeline = fetch >> (analyze_a & analyze_b) >> report\nrun(pipeline)","title":"Diamond pattern"},{"category":"section","location":"guide/composition/#Multi-stage-parallel","page":"Composing pipelines","text":"     ┌─ b ─┐     ┌─ e ─┐\n  a ─┤     ├─ d ─┤     ├─ g\n     └─ c ─┘     └─ f ─┘\n\na = @step a = sh\"step_a\"\nb = @step b = sh\"step_b\"\nc = @step c = sh\"step_c\"\nd = @step d = sh\"step_d\"\ne = @step e = sh\"step_e\"\nf = @step f = sh\"step_f\"\ng = @step g = sh\"step_g\"\n\npipeline = a >> (b & c) >> d >> (e & f) >> g","title":"Multi-stage parallel"},{"category":"section","location":"guide/composition/#Independent-branches","page":"Composing pipelines","text":"  ┌─ fetch_a >> process_a ─┐\n  ├─ fetch_b >> process_b ─┼── merge\n  └─ fetch_c >> process_c ─┘\n\nfetch_a = @step fetch_a = sh\"fetch sample_a\"\nprocess_a = @step process_a = sh\"process sample_a\"\n# ... similarly for b, c\nmerge = @step merge = sh\"merge results\"\n\nbranch_a = fetch_a >> process_a\nbranch_b = fetch_b >> process_b\nbranch_c = fetch_c >> process_c\npipeline = (branch_a & branch_b & branch_c) >> merge\n\nThis pattern is common for processing multiple samples independently before combining results.\n\nNext: Control flow — fallback, retry, branching, and timeouts.","title":"Independent branches"},{"category":"section","location":"#SimplePipelines.jl","page":"Home","text":"Minimal, type-stable DAG pipelines for Julia\n\nA step is a shell command or Julia function. Operators connect steps into a graph: >> (order), & (parallel), |> (pipe), and others. run executes the graph once; each step yields a StepResult (success, duration, outputs).\n\nAd-hoc Julia/shell glue Same thing as a pipeline\nRun A, then B, then C a >> b >> c\nTwo branches, then merge (a & b) >> merge\nRetry or alternate on failure step^3, x | y, Branch(…)\nShell + Julia in one workflow Same operators for sh\"…\" and function steps\n\nusing SimplePipelines\n\nfetch     = @step fetch = sh\"echo data > file.txt\"\nanalyze_a = @step a = sh\"wc -c file.txt\"\nanalyze_b = @step b = sh\"wc -l file.txt\"\nreport    = @step r = sh\"echo done\"\n\nfetch >> (analyze_a & analyze_b) >> report |> run\n\nrun(sh\"echo one\" >> sh\"echo two\")\nrun((sh\"A\" & sh\"B\") >> sh\"merge\")\n\nSteps and shell · Composing pipelines · Control flow · Fan-out and reduce · Running and inspecting · Examples · Quick reference · API","title":"SimplePipelines.jl"}]
}
