var documenterSearchIndex = {"docs":
[{"category":"section","location":"doctests/#Doctests","page":"Doctest blocks","text":"Runnable jldoctest blocks checked by Documenter.jl on every full doc build and from Pkg.test() via makedocs(doctest = :only) (see docs/make.jl and the DOCUMENTER_DOCTEST environment variable).","title":"Doctests"},{"category":"section","location":"doctests/#Shell-sequence","page":"Doctest blocks","text":"julia> using SimplePipelines\n\njulia> r = run(sh\"echo hi\" >> sh\"echo second\", verbose=false);\n\njulia> length(r)\n2\n\njulia> all(x -> x.success, r)\ntrue","title":"Shell sequence"},{"category":"section","location":"doctests/#Named-steps-and-parallel","page":"Doctest blocks","text":"julia> using SimplePipelines\n\njulia> a = @step a = sh\"echo A\";\n\njulia> b = @step b = sh\"echo B\";\n\njulia> r = run(a & b, verbose=false);\n\njulia> length(r)\n2\n\njulia> all(x -> x.success, r)\ntrue","title":"Named steps and parallel"},{"category":"section","location":"doctests/#Fallback","page":"Doctest blocks","text":"julia> using SimplePipelines\n\njulia> r = run((@step bad = sh\"false\") | (@step ok = sh\"echo ok\"), verbose=false);\n\njulia> r[end].success\ntrue","title":"Fallback"},{"category":"section","location":"doctests/#resolve-returns-NoWork-when-the-target-already-exists","page":"Doctest blocks","text":"julia> using SimplePipelines\n\njulia> nw = mktempdir() do dir\n           cd(dir) do\n               write(\"done.txt\", \"ok\")\n               r = @rule a([] => \"done.txt\") = \"echo y > {output}\"\n               resolve([r], [\"done.txt\"])\n           end\n       end;\n\njulia> nw isa NoWork\ntrue","title":"resolve returns NoWork when the target already exists"},{"category":"section","location":"development/#Extending-SimplePipelines","page":"Extending & dev workflow","text":"SimplePipelines is designed for easy extension via Julia's multiple dispatch.","title":"Extending SimplePipelines"},{"category":"section","location":"development/#Testing-and-coverage","page":"Extending & dev workflow","text":"Tests live under test/suites/ (included from test/runtests.jl) so each file stays a manageable size. Run the full suite:\n\njulia --project=. -e 'using Pkg; Pkg.test()'\n\nTable I/O tests need the optional test extras (CSV, DataFrames) declared in the root Project.toml [targets] test = [...].","title":"Testing and coverage"},{"category":"section","location":"development/#Building-the-docs","page":"Extending & dev workflow","text":"The docs project lives in docs/ (docs/Project.toml). docs/make.jl sets root = dirname(@__FILE__), so you can run it from any working directory.\n\njulia --project=docs -e 'using Pkg; Pkg.develop(path=\"..\"); Pkg.instantiate(); include(\"make.jl\")'\n\nRun from the repository root (so path=\"..\" resolves to the package), or use an absolute path to the package instead of \"..\".","title":"Building the docs"},{"category":"section","location":"development/#Doctests","page":"Extending & dev workflow","text":"Runnable examples live in docs/src/doctests.md as Documenter jldoctest fenced code blocks. They run whenever makedocs runs (see the doctest keyword in docs/make.jl).\n\nFull HTML build plus doctests: default DOCUMENTER_DOCTEST unset or true.\nDoctests only (no HTML; fast): set DOCUMENTER_DOCTEST=only before include(\"make.jl\"). This is what Pkg.test() runs in a subprocess so CI exercises the same checks as the doc build.\nTo skip doctests temporarily: DOCUMENTER_DOCTEST=false.\n\nRun tests with code coverage (for local profiling or CI):\n\njulia --project=. -e 'using Pkg; Pkg.test(coverage=true)'\n\nCoverage is reported in lcov.info; CI uploads it to Codecov.","title":"Doctests"},{"category":"section","location":"development/#Architecture","page":"Extending & dev workflow","text":"All nodes inherit from AbstractNode. To add custom behavior, define a new subtype and implement dispatch methods:\n\nAbstractNode\n    ├── Step{F}           # Leaf node\n    ├── Sequence          # Sequential (`Vector{AbstractNode}`)\n    ├── Parallel          # Concurrent (`Vector{AbstractNode}`)\n    ├── Retry{N}          # Retry on failure\n    ├── Fallback{A,B}     # Try A, else B\n    └── Branch{C,T,F}     # Conditional","title":"Architecture"},{"category":"section","location":"development/#Custom-Node-in-4-Steps","page":"Extending & dev workflow","text":"using SimplePipelines\nimport SimplePipelines: AbstractNode, RunContext, run_node, print_dag, count_steps, steps\n\n# 1. Define type (parametric for type stability)\nstruct Timeout{N<:AbstractNode} <: AbstractNode\n    node::N\n    seconds::Float64\nend\n\n# 2. Implement execution (match package signature: ctx, forced, context_input)\nfunction run_node(t::Timeout, ctx::RunContext, forced::Bool=false, context_input=nothing)\n    # ... timeout logic ...\n    return run_node(t.node, ctx, forced, context_input)\nend\n\n# 3. Implement visualization (signature: io, node, pre, cont, color)\nfunction print_dag(io::IO, t::Timeout, pre::String, cont::String, color::Bool)\n    print(io, pre, \"⏱ Timeout \", t.seconds, \"s\\n\")\n    print_dag(io, t.node, cont * \"    \", cont * \"    \", color)\nend\n\n# 4. Implement utilities\ncount_steps(t::Timeout) = count_steps(t.node)\nsteps(t::Timeout) = steps(t.node)","title":"Custom Node in 4 Steps"},{"category":"section","location":"development/#Custom-Operator","page":"Extending & dev workflow","text":"import Base: ^\n\n# Node^3 means repeat 3 times\n^(node::AbstractNode, n::Int) = Repeat(node, n)","title":"Custom Operator"},{"category":"section","location":"development/#Custom-Step-Executor","page":"Extending & dev workflow","text":"Extend execute for new work types:\n\nstruct HTTPGet\n    url::String\nend\n\nfunction SimplePipelines.execute(step::Step{HTTPGet})\n    start = time()\n    resp = HTTP.get(step.work.url)\n    return StepResult(step, resp.status == 200, time() - start, step.inputs, step.outputs, String(resp.body))\nend\n\n# Usage: Step(:fetch, HTTPGet(\"https://api.example.com\"))","title":"Custom Step Executor"},{"category":"section","location":"development/#Type-Stability-Rules","page":"Extending & dev workflow","text":"Use parametric types for leaf/runtime payloads (Step{F}, result wrappers, node decorators).\nUse Vector{AbstractNode} for wide child collections when encoding every child in the type would hurt compile time.\nDispatch on types, don't check with isa.","title":"Type Stability Rules"},{"category":"section","location":"examples/bioinformatics/#Examples:-bioinformatics","page":"Bioinformatics","text":"Longer domain workflows. These are cookbook sketches for readers who already know the basic DAG operators. Tool names are real; runs require installed software and inputs unless you substitute echo placeholders (see examples/bioinformatics.jl in the repo).\n\nMore examples: Basics · Control flow · Complex DAGs\n\nPattern: Multiple donors/samples — use ForEach with {wildcard} patterns so files are discovered and each match becomes a parallel branch.","title":"Examples: bioinformatics"},{"category":"section","location":"examples/bioinformatics/#4.1-Immune-repertoire-(single-donor)","page":"Bioinformatics","text":"Flow: Paired-end FASTQ → merge (PEAR) → FASTQ→FASTA → IgBLAST → Julia filter by identity.\n\n  R1.fastq ──┐\n             ├──►  pear  ──►  to_fasta  ──►  igblast  ──►  filter_identity  ──►  igblast_filtered.tsv\n  R2.fastq ──┘\n\nusing SimplePipelines\nusing DelimitedFiles\n\npear = @step pear = sh\"pear -f R1.fastq -r R2.fastq -o merged\"\nto_fasta = @step to_fasta = sh\"seqtk seq -A merged.assembled.fastq > merged.assembled.fasta\"\nigblast = @step igblast = sh\"igblastn -query merged.assembled.fasta -germline_db_V V.fasta -germline_db_D D.fasta -germline_db_J J.fasta -outfmt 7 -out igblast.tsv\"\n\nfilter_identity = @step filter_identity = () -> begin\n    data, header_row = readdlm(\"igblast.tsv\", '\\t', header=true)\n    header = vec(header_row)\n    v_idx = findfirst(isequal(\"v_identity\"), header)\n    j_idx = findfirst(isequal(\"j_identity\"), header)\n    to_float(x) = something(tryparse(Float64, string(x)), 0.0)\n    filtered = [r for r in eachrow(data) if to_float(r[v_idx]) > 90.0 && to_float(r[j_idx]) > 90.0]\n    writedlm(\"igblast_filtered.tsv\", vcat(header', filtered), '\\t')\n    return \"Filtered $(length(filtered)) sequences\"\nend\n\npipeline = pear >> to_fasta >> igblast >> filter_identity\nrun(Pipeline(pipeline, name=\"Immune Repertoire\"))","title":"4.1 Immune repertoire (single donor)"},{"category":"section","location":"examples/bioinformatics/#4.2-Immune-repertoire-(multiple-donors)","page":"Bioinformatics","text":"Flow: ForEach discovers fastq/{donor}_R1.fq.gz → one branch per donor.\n\n  ForEach(\"fastq/{donor}_R1.fq.gz\")\n       │\n       ├── donor1: pear → fasta → igblast  ──┐\n       ├── donor2: ...                      ──┼── (parallel)\n       └── donorN: ...                      ──┘\n\nusing SimplePipelines\n\npipeline = ForEach(\"fastq/{donor}_R1.fq.gz\") do donor\n    sh(\"pear -f fastq/$(donor)_R1.fq.gz -r fastq/$(donor)_R2.fq.gz -o $(donor)_merged\") >>\n    sh(\"seqtk seq -A $(donor)_merged.assembled.fastq > $(donor).fasta\") >>\n    sh(\"igblastn -query $(donor).fasta -germline_db_V V.fasta -germline_db_D D.fasta -germline_db_J J.fasta -outfmt 7 -out $(donor)_igblast.tsv\")\nend\n\nrun(pipeline)","title":"4.2 Immune repertoire (multiple donors)"},{"category":"section","location":"examples/bioinformatics/#4.3-Variant-calling","page":"Bioinformatics","text":"Flow: QC → trim → align → sort/index → call → index VCF → filter.\n\n  R1.fq.gz ──┐\n             ├──►  fastqc  ──►  trim  ──►  align  ──►  index  ──►  call  ──►  index_vcf  ──►  filter_vcf\n  R2.fq.gz ──┘\n\nusing SimplePipelines\n\nfastqc = @step fastqc = sh\"fastqc -o qc/ R1.fq.gz R2.fq.gz\"\ntrim = @step trim = sh\"trimmomatic PE R1.fq.gz R2.fq.gz R1_trimmed.fq.gz R1_unpaired.fq.gz R2_trimmed.fq.gz R2_unpaired.fq.gz ILLUMINACLIP:adapters.fa:2:30:10 LEADING:3 TRAILING:3 SLIDINGWINDOW:4:15 MINLEN:36\"\n\nalign = @step align = sh\"bwa mem -t 8 GRCh38.fa R1_trimmed.fq.gz R2_trimmed.fq.gz | samtools sort -@ 4 -o aligned.bam -\"\nindex = @step index = sh\"samtools index aligned.bam\"\n\ncall = @step call = sh\"bcftools mpileup -f GRCh38.fa aligned.bam | bcftools call -mv -Oz -o variants.vcf.gz\"\nindex_vcf = @step index_vcf = sh\"bcftools index variants.vcf.gz\"\nfilter_vcf = @step filter_vcf = sh\"bcftools filter -i 'QUAL>=20' variants.vcf.gz -Oz -o filtered.vcf.gz\"\n\npipeline = fastqc >> trim >> align >> index >> call >> index_vcf >> filter_vcf\nrun(Pipeline(pipeline, name=\"Variant Calling\"))","title":"4.3 Variant calling"},{"category":"section","location":"guide/rules-and-diagnostics/#Rules-and-diagnostics","page":"Rules and diagnostics","text":"Writing every sample by hand gets old quickly. Rules are for workflows where the same transformation applies to many files:\n\nraw/A.fq -> out/A.bam\nraw/B.fq -> out/B.bam\nraw/C.fq -> out/C.bam\n\nInstead of listing every step, write one pattern. The wildcard in the output path is inferred from the target you ask for.\n\nalign = @rule align(\"raw/{sample}.fq\" => \"out/{sample}.bam\") =\n    \"bwa mem ref.fa {input} > {output}\"\n\nRead this as: \"to make out/{sample}.bam, use raw/{sample}.fq and run this command.\"\n\nThe command is a template string. Unlike @step sort = sh\"sort data.txt\", a rule does not know its concrete filenames yet. {input}, {output}, and {sample} are filled in when you ask for a target such as out/A.bam.","title":"Rules and diagnostics"},{"category":"section","location":"guide/rules-and-diagnostics/#Check-One-Rule-First","page":"Rules and diagnostics","text":"Before composing a workflow, check the rule by itself:\n\ncheck(align)\n\nThis answers: what wildcards does the rule know about, and which placeholders does the command use?\n\nThen try one concrete target:\n\ncheck(align, \"out/A.bam\")\n\nNow the abstract pattern becomes concrete:\n\nsample  => A\ninput   => raw/A.fq\noutput  => out/A.bam\ncommand => bwa mem ref.fa raw/A.fq > out/A.bam\n\nNothing runs here. This is a safe way to catch spelling mistakes like {smaple} before building the whole DAG.","title":"Check One Rule First"},{"category":"section","location":"guide/rules-and-diagnostics/#Workflow-block","page":"Rules and diagnostics","text":"Once individual rules look right, collect them into a workflow block. A workflow is just a named set of rules plus the targets you want to build.\n\nwf = @workflow \"rnaseq\" begin\n    @rule source([] => \"raw/{sample}.fq\") =\n        \"mkdir -p raw && echo {sample} > {output}\"\n\n    @rule align(\"raw/{sample}.fq\" => \"out/{sample}.bam\") =\n        \"mkdir -p out && bwa mem ref.fa {input} > {output}\"\n\n    @targets \"out/{sample}.bam\" sample=[\"A\", \"B\"]\nend\n\n@targets expands the target pattern:\n\n@targets \"out/{sample}.bam\" sample=[\"A\", \"B\"]\n# [\"out/A.bam\", \"out/B.bam\"]\n\nThe workflow block keeps the beginner-facing syntax compact. Underneath, it is the same Workflow object used by plan(wf) and run(wf).","title":"Workflow block"},{"category":"section","location":"guide/rules-and-diagnostics/#Explain-the-DAG","page":"Rules and diagnostics","text":"Use explain when rules depend on other rules and you want to see the chain for one target.\n\nexplain(wf; target=\"out/A.bam\")\n\nFor the workflow above, explain shows that out/A.bam is made by align, and that align depends on raw/A.fq, which is made by source.\n\nAfter the explanation makes sense, inspect or run the DAG:\n\nprint_dag(plan(wf))\nrun(wf)","title":"Explain the DAG"},{"category":"section","location":"guide/rules-and-diagnostics/#What-To-Remember","page":"Rules and diagnostics","text":"Use check while writing one rule. Use explain after composing rules into a workflow. That keeps debugging local: first prove one pattern, then prove the dependency chain.\n\nNext: Fan-out and reduce covers item-driven fan-out when a collection or discovered files should drive the branches.","title":"What To Remember"},{"category":"section","location":"guide/control-flow/#Control-flow","page":"Control flow","text":"Control flow wraps an existing node with a policy: try again, use a backup, choose one branch, or stop waiting after a deadline. Use these when the workflow needs operational behavior, not as a substitute for clear DAG structure.","title":"Control flow"},{"category":"section","location":"guide/control-flow/#Use-A-Backup-With","page":"Control flow","text":"Fallback is useful when two methods can produce an acceptable result and the second should run only if the first fails.\n\nfast_method = @step fast = sh\"fast_tool input.txt\"\nslow_method = @step slow = sh\"slow_tool input.txt\"\n\npipeline = fast_method | slow_method\n\nThis is different from fast_method & slow_method: fallback does not spend time on the backup unless it is needed.","title":"Use A Backup With |"},{"category":"section","location":"guide/control-flow/#Retry-A-Flaky-Step","page":"Control flow","text":"flaky_api_call = @step api = sh\"echo 'mock response'\"\npipeline = flaky_api_call^3\n\nUse Retry when you want a delay:\n\nfetch = @step fetch = sh\"curl -L -o data.txt https://example.com/data.txt\"\npipeline = Retry(fetch, 5; delay=2.0)\n\nRetry composes with fallback:\n\npipeline = Retry(fetch, 3; delay=1.0) | @step cached = sh\"cp cache.txt data.txt\"","title":"Retry A Flaky Step"},{"category":"section","location":"guide/control-flow/#Choose-A-Branch","page":"Control flow","text":"large_file_pipeline = @step large = sh\"process_large data.txt\"\nsmall_file_pipeline = @step small = sh\"process_small data.txt\"\n\npipeline = Branch(\n    () -> filesize(\"data.txt\") > 1_000_000,\n    large_file_pipeline,\n    small_file_pipeline,\n)\n\nUse the @branch macro for the common zero-argument case:\n\nuse_fast = Ref(true)\npipeline = @branch use_fast[] fast_method slow_method\n\nWhen a branch follows a step that passes a value forward, the predicate can accept that value:\n\nemit_path = @step emit_path = () -> \"data.csv\"\ncsv_step = @step csv = sh\"process_csv data.csv\"\ntxt_step = @step txt = sh\"process_txt data.txt\"\n\npipeline = emit_path >> Branch(path -> endswith(path, \".csv\"), csv_step, txt_step)","title":"Choose A Branch"},{"category":"section","location":"guide/control-flow/#Stop-Waiting-With-Timeout","page":"Control flow","text":"long_running_step = @step long = sh\"sleep 1\"\npipeline = Timeout(long_running_step, 30.0)\n\nTimeout returns a failed result if the deadline passes. It reports :timed_out for a timeout and :inner_exception if the wrapped node throws before finishing. External shell subprocesses may continue after Julia stops waiting for them, so treat timeout as a pipeline-level failure signal rather than a guaranteed process killer.","title":"Stop Waiting With Timeout"},{"category":"section","location":"guide/control-flow/#What-To-Remember","page":"Control flow","text":"Keep the main DAG readable first. Add retry, fallback, branching, or timeout only where the workflow really needs that behavior.\n\nNext: Fan-out and reduce shows how to repeat a branch over many items and combine results.","title":"What To Remember"},{"category":"section","location":"guide/getting-started/#Getting-started","page":"Getting started","text":"This page builds one small pipeline from scratch. It is intentionally simple: create a file, sort it, then compute two summaries in parallel.\n\nThe point is not the shell commands. The point is the shape:\n\nprepare -> sort_file -> count_lines\n                    \\-> count_bytes","title":"Getting started"},{"category":"section","location":"guide/getting-started/#Define-The-Work","page":"Getting started","text":"A pipeline is made from steps. Each step has a name and some work to run. When a step reads or writes files, declare those files so the runtime can check inputs, verify outputs, and skip fresh work later.\n\nusing SimplePipelines\n\nprepare = @step prepare([] => [\"data.txt\"]) =\n    sh\"(echo line3; echo line1; echo line2) > data.txt\"\n\nsort_file = @step sort_file([\"data.txt\"] => [\"sorted.txt\"]) =\n    sh\"sort data.txt > sorted.txt\"\n\nThe first step has no input files and promises data.txt. The second step needs data.txt and promises sorted.txt.","title":"Define The Work"},{"category":"section","location":"guide/getting-started/#Add-Parallel-Work","page":"Getting started","text":"After the file is sorted, two summaries can run independently.\n\ncount_lines = @step count_lines([\"sorted.txt\"] => [\"lines.txt\"]) =\n    sh\"wc -l sorted.txt > lines.txt\"\n\ncount_bytes = @step count_bytes([\"sorted.txt\"] => [\"bytes.txt\"]) =\n    sh\"wc -c sorted.txt > bytes.txt\"","title":"Add Parallel Work"},{"category":"section","location":"guide/getting-started/#Compose-The-DAG","page":"Getting started","text":"Use >> for \"then\" and & for \"at the same time\".\n\npipeline = prepare >> sort_file >> (count_lines & count_bytes)\n\nThis is the whole DAG. You can inspect it before running:\n\nprint_dag(pipeline)","title":"Compose The DAG"},{"category":"section","location":"guide/getting-started/#Run-It","page":"Getting started","text":"results = run(pipeline)\n\nrun returns one StepResult per step that ran. The printed log is for you; the result vector is for code.\n\nall(r -> r.success, results)\nlast(results).outputs\n\nIf you run the same pipeline again, steps with fresh declared outputs can be skipped.","title":"Run It"},{"category":"section","location":"guide/getting-started/#Where-This-Goes-Next","page":"Getting started","text":"This example used fixed filenames. When filenames follow a pattern such as raw/{sample}.fq -> out/{sample}.bam, use rules and check them before running:\n\nalign = @rule align(\"raw/{sample}.fq\" => \"out/{sample}.bam\") =\n    \"bwa mem ref.fa {input} > {output}\"\n\ncheck(align, \"out/A.bam\")\n\nFor now, keep the mental model:\n\nsteps are units of work;\n>> and & draw the DAG;\nrun executes the DAG and returns structured results.\n\nNext: Steps and shell explains how to write steps well.","title":"Where This Goes Next"},{"category":"section","location":"api/#API-Reference","page":"API","text":"","title":"API Reference"},{"category":"section","location":"api/#Module","page":"API","text":"","title":"Module"},{"category":"section","location":"api/#Types","page":"API","text":"","title":"Types"},{"category":"section","location":"api/#Macros","page":"API","text":"","title":"Macros"},{"category":"section","location":"api/#Resource-aware-execution","page":"API","text":"Annotate heavy nodes with with_resources and pass memory_budget_mb / thread_budget to run so the parallel scheduler keeps total concurrent resource use under a soft cap. Wrap heavy values in FilePath and use materialize at the consumer to keep memory bounded between steps.","title":"Resource-aware execution"},{"category":"section","location":"api/#Output-side-wildcard-inference","page":"API","text":"Declare reusable Rules with {wildcard} patterns, then call resolve to walk a target list backward through the rule set and build a runnable DAG. Rule work supports both shell templates and (inputs, outputs, wildcards) -> Cmd | String | Function.\n\nexpand generates concrete target lists from a template by Cartesian product, and a Workflow bundles rules + default targets behind a single run(::Workflow) entry point.","title":"Output-side wildcard inference"},{"category":"section","location":"api/#Operators","page":"API","text":"The package extends these operators for pipeline composition. Cmd and Function arguments are auto-wrapped in Step.\n\nOperator Name Description\n>> Sequence Run in order; pass previous output to next (function step)\n& Parallel Run nodes concurrently\n| Fallback Run fallback if primary fails\n^ Retry Wrap with retries, e.g. node^3\n|> Pipe Run right with left's output(s) (single or vector)\n>>> SameInputPipe Run both with the same input (e.g. branch id)\n.>> BroadcastPipe Attach right to each branch of left (per-branch pipeline)\n\nWhen the left has one output, >>, |>, and .>> all pass that value to the next step. When the left has multiple outputs (ForEach, Parallel):\n\nLeft side  Pipe \nSingle output step(one value) step(one value) step(one value)\nMulti output step(last only) step(vector of all) step per branch (one call each)","title":"Operators"},{"category":"section","location":"api/#Functions","page":"API","text":"","title":"Functions"},{"category":"section","location":"api/#Shell","page":"API","text":"The string macro shell_raw\"...\" (and triple-quoted shell_raw\\\"\\\"\\\"...\\\"\\\"\\\") is documented in @shell_raw_str; use it for scripts where the dollar sign must not be interpreted by Julia.\n\nsh_pipe(cmd1, cmd2, ...) folds several Cmds into one OS-level pipeline, so stdout flows through OS pipes without Julia-side buffering between stages. The @step macro recognises sh_pipe and evaluates it eagerly, so the resulting Step's work field is a Base.AbstractCmd rather than a thunk.","title":"Shell"},{"category":"section","location":"api/#Execution","page":"API","text":"Execution is recursive: run(pipeline) calls run_node(root, ...) which dispatches on node type and recurses (Sequence in order, Parallel/ForEach with optional @spawn).","title":"Execution"},{"category":"section","location":"api/#Defaults","page":"API","text":"These are used as the default keyword argument values in run.","title":"Defaults"},{"category":"section","location":"api/#Freshness-and-state","page":"API","text":"State is stored in .pipeline_state as a fixed-layout, memory-mapped file. Completions are batched and written when run() finishes.","title":"Freshness and state"},{"category":"section","location":"api/#State-file-format","page":"API","text":"This section is for maintainers and advanced users. The stable user-facing API is is_fresh, clear_state!, and the state_path keyword to run. The binary layout helpers below document the current persistence internals; do not build workflow code against them.","title":"State file format"},{"category":"section","location":"api/#Utilities","page":"API","text":"","title":"Utilities"},{"category":"section","location":"api/#Index","page":"API","text":"","title":"Index"},{"category":"type","location":"api/#SimplePipelines.AbstractNode","page":"API","text":"AbstractNode\n\nAbstract supertype of all pipeline nodes (Step, Sequence, Parallel, Retry, Fallback, Branch, Timeout, Force, Reduce, ForEach, Pipe, SameInputPipe, BroadcastPipe). Constructors only build the struct; execution is via run, which builds a RunContext and dispatches to run_node(node, ctx, forced, context_input).\n\n\n\n\n\n","title":"SimplePipelines.AbstractNode"},{"category":"type","location":"api/#SimplePipelines.AbstractStepResult","page":"API","text":"Supertype of all step results; use for Vector{AbstractStepResult} (e.g. return of run).\n\n\n\n\n\n","title":"SimplePipelines.AbstractStepResult"},{"category":"type","location":"api/#SimplePipelines.StepFailure","page":"API","text":"StepFailure(kind, message; detail=\"\")\n\nStructured failure payload for unsuccessful steps and internal execution errors.\n\nkind is a symbol categorising the error (:missing_input, :missing_output, :process_failed, :exception, :timed_out, :inner_exception, :no_matches, …).\nmessage is a short human-readable summary.\ndetail holds optional extra context (e.g. stderr tail). It may be empty.\n\nThis replaces stringly-typed \"Error: ...\" results; formatting is deferred to showerror / display code.\n\n\n\n\n\n","title":"SimplePipelines.StepFailure"},{"category":"type","location":"api/#SimplePipelines.StepResult","page":"API","text":"StepResult(step, success, duration, inputs, outputs, result)\n\nResult of running one step. Type is StepResult{S, I, O, V}. Type-stable: no Any.\n\nFields (all real)\n\ninputs — Input file paths the step declared. Empty for steps that take no input files (e.g. download / start nodes).\noutputs — Output file paths the step declared (files the step produces).\nresult — Execution result: stdout for shell steps, return value for function steps. Wrap large values in FilePath so the in-memory result stays small.\n\n\n\n\n\n","title":"SimplePipelines.StepResult"},{"category":"type","location":"api/#SimplePipelines.Step","page":"API","text":"Step{F} <: AbstractNode\n\nA single unit of work in a pipeline. F is the work type: Cmd (backtick or sh\"...\"), Function, or ShRun (from sh(f)).\n\nFields\n\nname::Symbol — Step identifier (auto-generated if not provided)\nwork::F — The command or function to execute\ninputs::Vector{String} — Input file dependencies\noutputs::Vector{String} — Output file paths\n\nConstructors\n\nStep(work)                           # Auto-generated name\nStep(name::Symbol, work)             # Named step\nStep(name, work, inputs, outputs)    # Full specification\n@step name = work                    # Macro form\n@step name(inputs => outputs) = work # Macro with dependencies\n\nSee also: @step, is_fresh, Force\n\n\n\n\n\n","title":"SimplePipelines.Step"},{"category":"type","location":"api/#SimplePipelines.ShRun","page":"API","text":"ShRun{F}\n\nInternal callable wrapper around a thunk that returns a shell command string at execution time. Use sh(cmd_func) in steps; calling sr() invokes sr.f() and returns the command string. The pipeline prints the command when verbose=true.\n\n\n\n\n\n","title":"SimplePipelines.ShRun"},{"category":"type","location":"api/#SimplePipelines.Sequence","page":"API","text":"Sequence <: AbstractNode\na >> b\n\nExecutes nodes sequentially, stopping on first failure. Data passing: the next node receives one value: the previous step's output (declared output paths when applicable, else the step's result). When the previous node produced multiple results (e.g. Parallel/ForEach), the next node receives only the last branch's output. Distinct from |> (which passes a vector of all) and .>> (which runs the next step once per branch).\n\nChildren are stored in a Vector{AbstractNode} so Sequence scales to thousands of children without compile-time tuple-type blow-up.\n\n\n\n\n\n","title":"SimplePipelines.Sequence"},{"category":"type","location":"api/#SimplePipelines.Parallel","page":"API","text":"Parallel <: AbstractNode\n\nExecutes nodes concurrently using threads. Created automatically by the & operator. Children are stored in a Vector{AbstractNode} so wide fan-outs (hundreds of samples) do not blow up compile time.\n\n\n\n\n\n","title":"SimplePipelines.Parallel"},{"category":"type","location":"api/#SimplePipelines.Retry","page":"API","text":"Retry{N} <: AbstractNode\n\nRetries a node up to max_attempts times on failure, with optional delay. Created by the ^ operator or Retry() constructor.\n\n\n\n\n\n","title":"SimplePipelines.Retry"},{"category":"type","location":"api/#SimplePipelines.Fallback","page":"API","text":"Fallback{A,B} <: AbstractNode\n\nExecutes fallback node if primary fails. Created by the | operator.\n\n\n\n\n\n","title":"SimplePipelines.Fallback"},{"category":"type","location":"api/#SimplePipelines.Branch","page":"API","text":"Branch{C,T,F} <: AbstractNode\n\nConditional execution: at run time, condition() must return a Bool; the corresponding branch runs. For a clearer call site than Branch(() -> cond, a, b), see @branch.\n\n\n\n\n\n","title":"SimplePipelines.Branch"},{"category":"type","location":"api/#SimplePipelines.Timeout","page":"API","text":"Timeout{N} <: AbstractNode\n\nWraps a node with a time limit. If the deadline passes before the inner node finishes, the synthetic timeout step fails with StepFailure kind :timed_out. If the inner node throws (including from Branch before children run), the result uses kind :inner_exception. External processes started under sh / Cmd are not reliably terminated on interrupt.\n\n\n\n\n\n","title":"SimplePipelines.Timeout"},{"category":"type","location":"api/#SimplePipelines.Reduce","page":"API","text":"Reduce{F,N} <: AbstractNode\n\nRuns a parallel node and combines successful step outputs with a reducer function.\n\n\n\n\n\n","title":"SimplePipelines.Reduce"},{"category":"type","location":"api/#SimplePipelines.Force","page":"API","text":"Force{N} <: AbstractNode\n\nForces execution of a node, bypassing freshness checks. See also is_fresh, clear_state!.\n\n\n\n\n\n","title":"SimplePipelines.Force"},{"category":"type","location":"api/#SimplePipelines.Pipeline","page":"API","text":"Pipeline{N<:AbstractNode}\n\nA named pipeline wrapping a root node for execution.\n\n\n\n\n\n","title":"SimplePipelines.Pipeline"},{"category":"type","location":"api/#SimplePipelines.Pipe","page":"API","text":"Pipe{A, B} <: AbstractNode\na |> b\n\nRun a, then run b with a's output(s) as b's input. RHS must be a function step. Same notion of output as >> (declared output paths when applicable, else result). Single result → one value; multiple results (ForEach/Parallel) → vector of all branch outputs. Distinct from >> (which passes only the last) and .>> (which runs the next step once per branch).\n\n\n\n\n\n","title":"SimplePipelines.Pipe"},{"category":"type","location":"api/#SimplePipelines.SameInputPipe","page":"API","text":"SameInputPipe{A, B} <: AbstractNode\na >>> b\n\nRun a then b; both receive the same context input. b does not receive a's output. Distinct from >> and |> (where b receives a's output). Use when the next step should run on the same input (e.g. branch id) as the previous.\n\n\n\n\n\n","title":"SimplePipelines.SameInputPipe"},{"category":"type","location":"api/#SimplePipelines.BroadcastPipe","page":"API","text":"BroadcastPipe{A, B} <: AbstractNode\na .>> b   (broadcast of >>)\n\nRun the next step once per branch of the left node, each with that branch's output. For ForEach or Parallel, branches run in parallel. For a single-output node, equivalent to a >> b. Distinct from >> (one run, last branch only) and |> (one run with vector of all outputs).\n\n\n\n\n\n","title":"SimplePipelines.BroadcastPipe"},{"category":"type","location":"api/#SimplePipelines.Resources","page":"API","text":"Resources(; mem_mb=0, threads=1)\n\nResource hint for a node. mem_mb is the peak in-memory footprint (megabytes) expected while the node runs; threads is the number of CPU threads it intends to use. A 0 field means \"do not charge against the corresponding budget\".\n\n\n\n\n\n","title":"SimplePipelines.Resources"},{"category":"type","location":"api/#SimplePipelines.Resourced","page":"API","text":"Resourced{N} <: AbstractNode\n\nWraps a node with Resources. The runtime acquires the matching budget slots on the RunContext before executing the wrapped node, releasing them on completion.\n\n\n\n\n\n","title":"SimplePipelines.Resourced"},{"category":"type","location":"api/#SimplePipelines.FilePath","page":"API","text":"FilePath(path::String)\n\nWrapper marking that a step's value lives on a user-named file at path. Steps that write their output to a known path should return FilePath(...) so the runtime holds only a path between steps. The default materialize reads raw bytes; users can specialise for typed loading (CSV → DataFrame, etc.).\n\n\n\n\n\n","title":"SimplePipelines.FilePath"},{"category":"type","location":"api/#SimplePipelines.SpilledValue","page":"API","text":"SpilledValue(path::String)\n\nWrapper marking that a step's value was auto-spilled to a runtime-managed tempfile at path. Created by the runtime when auto_spill is on and a step's result exceeds spill_threshold_bytes. materialize(::SpilledValue) deserialises the value via Base.Serialization.\n\nDistinct from FilePath: FilePath is for user-named output files (arbitrary contents, raw-bytes default loader), SpilledValue is for serialised Julia objects the runtime owns.\n\n\n\n\n\n","title":"SimplePipelines.SpilledValue"},{"category":"type","location":"api/#SimplePipelines.SpilledStdout","page":"API","text":"SpilledStdout(path::String)\n\nWrapper for a shell step's captured stdout that was streamed directly to disk rather than buffered in RAM. The runtime emits this when an auto_spill-enabled shell step's output file exceeds spill_threshold_bytes, so peak memory is independent of the command's output size. materialize(::SpilledStdout) reads the file back as a String.\n\nDistinct from SpilledValue: SpilledValue stores a Base.Serialization blob of an arbitrary Julia object; SpilledStdout is raw bytes from a captured process — never serialised, never deserialised.\n\n\n\n\n\n","title":"SimplePipelines.SpilledStdout"},{"category":"type","location":"api/#SimplePipelines.Rule","page":"API","text":"Rule(name, inputs, outputs, work)\n\nA reusable production recipe. inputs and outputs are vectors of pattern strings (e.g. \"data/{sample}.fq\"). work is either:\n\nString: a shell command template using {input}, {output}, {wildcard} placeholders.\nFunction(inputs, outputs, wildcards): builds a node (Step / Cmd / function) at resolve time.\n\nUse @rule for ergonomic construction, and resolve to turn a set of rules + targets into a runnable DAG.\n\n\n\n\n\n","title":"SimplePipelines.Rule"},{"category":"type","location":"api/#SimplePipelines.NoWork","page":"API","text":"NoWork() <: AbstractNode\n\nSentinel node that performs no work and produces no step results. Returned by resolve when every target is already on disk.\n\n\n\n\n\n","title":"SimplePipelines.NoWork"},{"category":"macro","location":"api/#SimplePipelines.@step","page":"API","text":"@step name = work\n@step name(inputs => outputs) = work\n@step work\n\nCreate a named step with optional file dependencies. Steps are lazy: if the right-hand side is a function call (other than sh(...)), it is wrapped in a thunk and runs only when the pipeline is run via run(pipeline).\n\nUse sh\"...\" for literal commands; use sh(\"... \" * var * \" ...\") when you need interpolation at construction time. For commands built at run time, use sh(cmd_func) where cmd_func returns the command string; with verbose=true, the command is printed before execution. For shell scripts that use shell variables, use shell_raw\"...\" or shell_raw\"\"\"...\"\"\" (multiline) so Julia does not interpret the dollar sign.\n\nExamples\n\n@step download = sh\"curl -o data.csv http://example.com\"\n@step download([] => [\"data.csv\"]) = sh(\"curl -L -o \" * repr(path) * \" \" * url)\n@step process([\"input.csv\"] => [\"output.csv\"]) = sh\"sort input.csv > output.csv\"\n@step call_tool([ref, bams] => [out]) = sh(() -> \"bcftools mpileup -f \" * repr(ref) * \" \" * join(repr.(bams), \" \"))\n@step process(\"path\") = process_file   # function by name, receives path at run time\n@step sh\"echo hello\"\n\n\n\n\n\n","title":"SimplePipelines.@step"},{"category":"macro","location":"api/#SimplePipelines.@branch","page":"API","text":"@branch condition iftrue iffalse\n\nBuilds a Branch: at run time evaluates condition (must yield a Bool) and runs one side. Equivalent to Branch(() -> condition, …) with lifting for Cmd / Function operands.\n\nWhen the branch sits after a step that passes data (e.g. in a Sequence), use Branch((ctx) -> …, iftrue, iffalse) so the predicate receives that value; with no upstream value the runtime calls condition() with no arguments.\n\nExample\n\nuse_fast = Ref(true)\npipeline = @branch use_fast[] fast_step slow_step\n\n\n\n\n\n","title":"SimplePipelines.@branch"},{"category":"macro","location":"api/#SimplePipelines.@rule","page":"API","text":"@rule name(inputs => outputs) = work\n\nConstruct a Rule. Mirrors @step but uses patterns rather than concrete paths.\n\nExamples\n\nalign = @rule align(\"data/{sample}.fq\" => \"out/{sample}.bam\") =\n    \"bwa mem ref.fa {input} > {output}\"\nindex = @rule index(\"out/{sample}.bam\" => \"out/{sample}.bam.bai\") =\n    \"samtools index {input}\"\n\nplan = resolve([align, index], [\"out/A.bam.bai\", \"out/B.bam.bai\"])\nrun(plan)\n\n\n\n\n\n","title":"SimplePipelines.@rule"},{"category":"macro","location":"api/#SimplePipelines.@sh_str","page":"API","text":"sh\"command\"\nsh(command::String)\n\nShell commands: use sh\"...\" for literals, sh(\"...\") when you need interpolation.\n\nExamples\n\nsh\"sort data.txt > sorted.txt\"\nsh(\"process \\$(sample)_R1.fq\")\n\n\n\n\n\n","title":"SimplePipelines.@sh_str"},{"category":"macro","location":"api/#SimplePipelines.@shell_raw_str","page":"API","text":"shell_raw\"command\"\nshell_raw\"\"\"...\"\"\"\n\nString literal for shell commands where the dollar sign is not interpreted by Julia. Use for scripts that use shell variables. Triple-quoted form keeps multiline scripts readable; shell variables (e.g. $VAR, var) stay intact.\n\nExample (multiline with loop and Julia interpolation)\n\ndonors = [\"A\", \"B\"]\ncmd = shell_raw\"\"\"\nfor d in \"\"\" * join(donors, \" \") * shell_raw\"\"\"\n  do\n    echo \"Processing $d\"\n  done\n\"\"\"\n# Use in a step: sh(() -> cmd)\n\n\n\n\n\n","title":"SimplePipelines.@shell_raw_str"},{"category":"function","location":"api/#SimplePipelines.with_resources","page":"API","text":"with_resources(node; mem_mb=0, threads=1) -> Resourced\nwith_resources(cmd; ...) -> Resourced  (lifts Cmd via Step)\nwith_resources(f::Function; ...) -> Resourced  (lifts Function via Step)\n\nAnnotate a node with resource hints used by the parallel scheduler.\n\n\n\n\n\n","title":"SimplePipelines.with_resources"},{"category":"function","location":"api/#SimplePipelines.materialize","page":"API","text":"materialize(value)\n\nIdentity for plain values. For FilePath, reads raw bytes from disk (extend for typed loaders). For SpilledValue, deserialises the value back into the original Julia object. For SpilledStdout, reads the streamed-to-disk shell stdout back as a String.\n\n\n\n\n\n","title":"SimplePipelines.materialize"},{"category":"function","location":"api/#SimplePipelines.materialize_table","page":"API","text":"materialize_table(fp::FilePath; kwargs...)\n\nRead a CSV file from disk as a DataFrame (via CSV.jl). Load CSV and DataFrames after SimplePipelines so the package extension is active; otherwise this function has no methods and you get MethodError.\n\nKeyword arguments are passed through to CSV.read.\n\n\n\n\n\n","title":"SimplePipelines.materialize_table"},{"category":"function","location":"api/#SimplePipelines.check","page":"API","text":"check(rule)\ncheck(rule, target)\n\nInspect rule patterns without running work. check(rule) reports wildcard names, input/output patterns, and placeholders used by string work templates. check(rule, target) matches one concrete target and reports the inferred wildcards, concrete inputs/outputs, and rendered shell command when available.\n\n\n\n\n\n","title":"SimplePipelines.check"},{"category":"macro","location":"api/#SimplePipelines.@targets","page":"API","text":"@targets \"out/{sample}.bam\" sample=[\"A\", \"B\"]\n@targets [\"out/{sample}.bam\", \"qc/{sample}.html\"] sample=[\"A\", \"B\"]\n\nGenerate concrete target paths from wildcard values. This is macro sugar over expand for use inside @workflow blocks or push!(wf, ...).\n\n\n\n\n\n","title":"SimplePipelines.@targets"},{"category":"macro","location":"api/#SimplePipelines.@workflow","page":"API","text":"@workflow \"name\" begin\n    @rule ...\n    @targets ...\nend\n\nBuild a Workflow from rule and target entries. The macro is shallow: entries are still ordinary @rule and @targets forms, collected with push! into a Workflow.\n\n\n\n\n\n","title":"SimplePipelines.@workflow"},{"category":"type","location":"api/#SimplePipelines.RuleCheck","page":"API","text":"check(rule)\ncheck(rule, target)\n\nInspect a rule without running it. check(rule) shows the wildcard shape and template placeholders; check(rule, target) tests one concrete target and shows the wildcards, inputs, outputs, and rendered shell command when the rule work is a string template.\n\n\n\n\n\n","title":"SimplePipelines.RuleCheck"},{"category":"type","location":"api/#SimplePipelines.RuleInstantiationCheck","page":"API","text":"RuleInstantiationCheck\n\nDiagnostic returned by check(rule, target). It records the concrete target, wildcards inferred from the rule output pattern, substituted input/output paths, and a rendered command preview for string-template rules.\n\n\n\n\n\n","title":"SimplePipelines.RuleInstantiationCheck"},{"category":"type","location":"api/#SimplePipelines.RuleExplanationStep","page":"API","text":"RuleExplanationStep\n\nOne resolved rule in a PlanExplanation, including inferred wildcards, concrete paths, rendered command preview, and dependency targets.\n\n\n\n\n\n","title":"SimplePipelines.RuleExplanationStep"},{"category":"type","location":"api/#SimplePipelines.PlanExplanation","page":"API","text":"PlanExplanation\n\nDiagnostic returned by explain(rules, target) or explain(workflow; target). It lists the rule chain needed to produce the requested target without running the pipeline.\n\n\n\n\n\n","title":"SimplePipelines.PlanExplanation"},{"category":"function","location":"api/#SimplePipelines.resolve","page":"API","text":"resolve(rules, targets) -> AbstractNode\n\nBuild a DAG that produces every path in targets, working backward through rules. A target that already exists on disk is treated as satisfied (no node emitted for it). Multiple targets sharing a dependency reuse the same Step instance, so the runtime DAG protocol executes that dependency exactly once.\n\nIf every requested target already exists on disk, returns a no-op node (NoWork) that produces no step results when run — making \"rerun the pipeline; everything's fresh\" friendly instead of an error.\n\nThrows if a non-existent target has no producing rule, or if rule resolution forms a cycle.\n\n\n\n\n\n","title":"SimplePipelines.resolve"},{"category":"function","location":"api/#SimplePipelines.expand","page":"API","text":"expand(template, wildcards) -> Vector{String}\nexpand(template; wildcard1=values1, wildcard2=values2, ...) -> Vector{String}\nexpand(templates::AbstractVector, ...) -> Vector{String}\n\nGenerate concrete paths from one or more pattern templates by taking the Cartesian product of the wildcard value lists. wildcards may be a NamedTuple or supplied as keyword arguments; values are anything that can be turned into a string via string.\n\nExamples\n\nexpand(\"out/{sample}.bam\"; sample=[\"A\",\"B\",\"C\"])\n# [\"out/A.bam\", \"out/B.bam\", \"out/C.bam\"]\n\nexpand(\"out/{s}.{ext}\"; s=[\"A\",\"B\"], ext=[\"bam\",\"bai\"])\n# [\"out/A.bam\", \"out/A.bai\", \"out/B.bam\", \"out/B.bai\"]\n\nexpand([\"raw/{s}.fq\", \"qc/{s}.html\"]; s=[\"A\",\"B\"])\n# [\"raw/A.fq\", \"raw/B.fq\", \"qc/A.html\", \"qc/B.html\"]\n\nWhen there are multiple wildcards, Iterators.product is used so combinations are generated with the first keyword varying slowest and the last varying fastest.\n\n\n\n\n\n","title":"SimplePipelines.expand"},{"category":"type","location":"api/#SimplePipelines.Workflow","page":"API","text":"Workflow(; name=\"workflow\") -> Workflow\n\nA registry of Rules and default targets. Sugar over resolve(rules, targets) that lets a user describe a pipeline as one object:\n\nwf = Workflow(name=\"rnaseq\")\npush!(wf,\n    @rule align(\"raw/{s}.fq\" => \"out/{s}.bam\") = \"bwa mem ref.fa {input} > {output}\",\n    @rule index(\"out/{s}.bam\" => \"out/{s}.bam.bai\") = \"samtools index {input}\")\npush!(wf, expand(\"out/{s}.bam.bai\"; s=[\"A\",\"B\",\"C\"]))\nrun(wf)\n\nItems pushed to a workflow are dispatched by type: Rule items go to wf.rules, strings (or vectors of strings) go to wf.targets. You can override targets at run time: run(wf; targets=[\"out/A.bam.bai\"]).\n\n\n\n\n\n","title":"SimplePipelines.Workflow"},{"category":"function","location":"api/#SimplePipelines.plan","page":"API","text":"plan(wf::Workflow; targets=wf.targets) -> AbstractNode\n\nResolve the workflow's rules against targets, returning a node ready to run. Useful for inspecting the build plan with print_dag before execution.\n\n\n\n\n\n","title":"SimplePipelines.plan"},{"category":"function","location":"api/#SimplePipelines.explain","page":"API","text":"explain(rules, target)\nexplain(workflow; target)\n\nExplain how a concrete target is resolved through one or more rules without running the pipeline. The result shows each matched rule, inferred wildcards, concrete inputs/outputs, rendered string command, and rule dependencies.\n\n\n\n\n\n","title":"SimplePipelines.explain"},{"category":"type","location":"api/#SimplePipelines.ForEach","page":"API","text":"Lazy node: run block over file matches (pattern string) or over a collection (vector). Dispatches on second argument.\n\n\n\n\n\n","title":"SimplePipelines.ForEach"},{"category":"type","location":"api/#SimplePipelines.fe","page":"API","text":"Short alias for ForEach. Use fe(\"pattern\") do x ... end.\n\n\n\n\n\n","title":"SimplePipelines.fe"},{"category":"function","location":"api/#SimplePipelines.sh","page":"API","text":"Shell command with string (build-time). For run-time command use sh(cmd_func). See also @sh_str.\n\n\n\n\n\n","title":"SimplePipelines.sh"},{"category":"function","location":"api/#SimplePipelines.sh_pipe","page":"API","text":"sh_pipe(cmds::Base.AbstractCmd...) -> Base.AbstractCmd\n\nCompose two or more shell commands into one OS-level pipeline. The resulting AbstractCmd runs all stages as a single Base.run, with stdout flowing through OS pipes between stages — no Julia-side buffering between commands. Only the final stage's stdout is captured by the runtime.\n\nThis is an alias for Base.pipeline; the package recognises it inside @step so the call is not thunked.\n\nExample\n\n@step align([\"foo.bam\"] => [\"filtered.txt\"]) =\n    sh_pipe(sh\"samtools view foo.bam\", sh\"awk -F'\\t' '$5>30'\", sh\"sort > filtered.txt\")\n\nCompare with the materialising >> form, where each step captures its full stdout into Julia memory before the next step runs.\n\nSee also: @step, @sh_str.\n\n\n\n\n\n","title":"SimplePipelines.sh_pipe"},{"category":"function","location":"api/#Base.run","page":"API","text":"run(p::Pipeline; verbose=true, dry_run=false, force=false,\n    jobs=default_jobs(), memory_budget_mb=default_memory_budget_mb(),\n    thread_budget=default_thread_budget(),\n    auto_spill=true, spill_threshold_bytes=default_spill_threshold_bytes(),\n    spill_dir=tempdir(), report=nothing) -> Vector{AbstractStepResult}\nrun(node::AbstractNode; kwargs...) -> Vector{AbstractStepResult}\n\nExecute a pipeline or node, returning a StepResult for every step that ran.\n\nKeywords\n\nverbose=true: Show colored progress output.\ndry_run=false: Show DAG structure without executing.\nforce=false: Run all steps regardless of freshness.\njobs: Max concurrent branches for Parallel/ForEach. Defaults to min(Threads.nthreads(), 8) so a fan-out with N threads cannot oversubscribe the machine. Pass jobs=0 to disable (unbounded — only do this if every branch is annotated with with_resources).\nmemory_budget_mb: Soft cap (MB) on concurrent memory across nodes wrapped with with_resources. Defaults to 50% of total system RAM. Branches with declared mem_mb block on a semaphore until enough budget is free. Pass 0 to disable.\nthread_budget: Soft cap on concurrent CPU threads across nodes wrapped with with_resources. Defaults to default_thread_budget, which matches jobs. Pass 0 to disable.\nauto_spill=true: Two complementary behaviours. (1) Shell steps: stdout streams directly to a tempfile in spill_dir while the command runs, so peak RAM is bounded by the OS pipe buffer rather than the command's total output. After the process exits, if the file is below spill_threshold_bytes it is read back as a String and the tempfile is deleted (preserving in-RAM semantics for typical short outputs); above threshold, r.result becomes a SpilledStdout wrapper that materialize reads back as a String on demand. (2) Function steps: after the step returns, if Base.summarysize(r.result) > spill_threshold_bytes, the value is serialised to a tempfile and r.result is replaced with a SpilledValue. Together these keep the per-run memo's RAM footprint bounded regardless of step count or command output size. Pass auto_spill=false to keep every result in RAM (shell steps revert to the legacy IOBuffer capture).\nspill_threshold_bytes: Spill threshold; defaults to 10 MB.\nspill_dir: Where the spill tempfiles live; defaults to tempdir(). Pass a mktempdir() if you want auto-cleanup at scope exit.\n\nMemory safety\n\nThe package treats disk as effectively infinite, RAM as finite. Three defaults combine to make a default-run pipeline memory-safe by construction:\n\njobs caps the live-execution set at the host's thread count.\nauto_spill swaps each finished step's big result for a tiny SpilledValue so the memo doesn't accumulate raw values.\nmemory_budget_mb and thread_budget serialise declared-heavy steps via with_resources.\n\nDownstream consumers call materialize to retrieve a SpilledValue or FilePath. Small step results (below threshold) stay in RAM with no I/O cost.\n\nThe returned vector contains every step's result. If you only want the terminal value, take last(results); if you want successes, filter(r -> r.success, results).\n\nreport: optional report(results; pipeline::String) called after the DAG finishes and before state is saved. Use for summaries, artifact registries, or CI badges.\n\nSee also: is_fresh, Force, print_dag, with_resources, FilePath, SpilledValue, materialize, default_jobs, default_memory_budget_mb, default_thread_budget.\n\n\n\n\n\n","title":"Base.run"},{"category":"function","location":"api/#SimplePipelines.default_jobs","page":"API","text":"default_jobs() -> Int\n\nSafe default for the number of concurrent parallel/foreach branches: at most as many as Threads.nthreads(), capped at 8. Spawning more branches than threads just queues work without speeding it up, but it does multiply memory pressure.\n\n\n\n\n\n","title":"SimplePipelines.default_jobs"},{"category":"function","location":"api/#SimplePipelines.default_memory_budget_mb","page":"API","text":"default_memory_budget_mb() -> Int\n\nSafe default soft cap for the per-run memory budget: 50% of total system RAM, in MB. Steps wrapped with with_resources charge the budget; un-annotated steps don't, so this is only an upper bound on declared concurrent memory. To disable the cap entirely, pass memory_budget_mb=0 to run.\n\nOverride at the user level by passing memory_budget_mb=N to run(...).\n\n\n\n\n\n","title":"SimplePipelines.default_memory_budget_mb"},{"category":"function","location":"api/#SimplePipelines.default_thread_budget","page":"API","text":"default_thread_budget() -> Int\n\nSafe default soft cap for declared CPU-thread use. It matches default_jobs, so resourced nodes that declare threads cannot collectively exceed the same host-aware cap used for parallel branch scheduling. Pass thread_budget=0 to run to disable this cap.\n\n\n\n\n\n","title":"SimplePipelines.default_thread_budget"},{"category":"function","location":"api/#SimplePipelines.default_spill_threshold_bytes","page":"API","text":"default_spill_threshold_bytes() -> Int\n\nDefault size above which a step's in-memory result is auto-spilled to disk. 10 MB — small enough to spill anything DataFrame-sized, large enough to keep small dictionaries / counters / arrays in RAM where the I/O cost would dominate.\n\n\n\n\n\n","title":"SimplePipelines.default_spill_threshold_bytes"},{"category":"function","location":"api/#SimplePipelines.is_fresh","page":"API","text":"is_fresh(step::Step) -> Bool\nis_fresh(step::Step, ctx::RunContext) -> Bool\n\nCheck if a step can be skipped based on Make-like freshness rules.\n\nFreshness Rules\n\nHas inputs and outputs: Fresh if all outputs exist and are newer than all inputs.\nHas only outputs: Fresh if outputs exist and step was previously completed.\nHas only inputs: Not fresh if any input file is missing; otherwise fresh if the step was previously completed (state cannot detect edited inputs without declared outputs).\nNo file dependencies: Fresh if step was previously completed (state-based tracking).\n\nState is persisted in .pipeline_state. Completions during a run are batched and written once when run() finishes.\n\nSee also: Force, clear_state!\n\n\n\n\n\n","title":"SimplePipelines.is_fresh"},{"category":"function","location":"api/#SimplePipelines.clear_state!","page":"API","text":"clear_state!()\n\nRemove the pipeline state file (.pipeline_state), forcing all steps to run on the next execution regardless of freshness.\n\nSee also: is_fresh, Force\n\n\n\n\n\n","title":"SimplePipelines.clear_state!"},{"category":"module","location":"api/#SimplePipelines.StateFormat","page":"API","text":"StateFormat\n\nFixed binary layout for the pipeline state file. Single source of truth for offsets, sizes, and all I/O: memory-mapped file with random access to the hash array.\n\nLayout (on disk):   [0..8)   magic (8 bytes)   [8..16)  count (UInt64, number of valid hashes)   [16..)   hashes (max_hashes × sizeof(UInt64), mmap-able for random access)\n\nAll read/write/mmap of the state file goes through this module (stateinit, stateread, statewrite, stateappend).\n\n\n\n\n\n","title":"SimplePipelines.StateFormat"},{"category":"type","location":"api/#SimplePipelines.StateFormat.StateFileLayout","page":"API","text":"StateFileLayout\n\nDescriptor for the fixed binary layout of the state file. Holds magic bytes, header length, offsets for count and hashes, and max capacity.\n\n\n\n\n\n","title":"SimplePipelines.StateFormat.StateFileLayout"},{"category":"function","location":"api/#SimplePipelines.StateFormat.state_init","page":"API","text":"Ensure the state file exists and has the correct size; create or truncate if needed.\n\n\n\n\n\n","title":"SimplePipelines.StateFormat.state_init"},{"category":"function","location":"api/#SimplePipelines.StateFormat.state_read","page":"API","text":"Read completed step hashes from the state file (memory-mapped, random access). Returns Set{UInt64}.\n\n\n\n\n\n","title":"SimplePipelines.StateFormat.state_read"},{"category":"function","location":"api/#SimplePipelines.StateFormat.state_write","page":"API","text":"Write all completed hashes to the state file (capped at layout.max_hashes).\n\n\n\n\n\n","title":"SimplePipelines.StateFormat.state_write"},{"category":"function","location":"api/#SimplePipelines.StateFormat.state_append","page":"API","text":"Append one hash; returns false if at capacity (caller should statewrite(union(stateread(), [h]))).\n\n\n\n\n\n","title":"SimplePipelines.StateFormat.state_append"},{"category":"function","location":"api/#SimplePipelines.count_steps","page":"API","text":"count_steps(node) -> Int\n\nNumber of distinct leaf steps that may execute. Shared sub-DAGs are counted once. For Branch, returns the maximum of the two branches (only one runs); for Reduce, includes the synthetic reducer step.\n\n\n\n\n\n","title":"SimplePipelines.count_steps"},{"category":"function","location":"api/#SimplePipelines.steps","page":"API","text":"steps(node) -> Vector{Step}\n\nReturn all leaf steps in the DAG (flattened). Shared sub-graphs are listed once. Composite nodes recurse via node_children; leaf nodes specialise.\n\n\n\n\n\n","title":"SimplePipelines.steps"},{"category":"function","location":"api/#SimplePipelines.print_dag","page":"API","text":"print_dag(node [; color=true])\nprint_dag(io, node [, indent])\n\nPrint a tree visualization of the pipeline DAG. With color=true (default when writing to a terminal), uses colors for node types and status. Steps with no inputs (start nodes) are shown with ◆ in light cyan; steps with inputs use ○ in cyan. See also run and display(pipeline).\n\n\n\n\n\n","title":"SimplePipelines.print_dag"},{"category":"section","location":"examples/complex-dags/#Examples:-complex-DAGs","page":"Complex DAGs","text":"Cookbook examples for larger DAG shapes. These are not the first tutorial; they are here once >>, &, retry/fallback, and branching already feel familiar.\n\nMore examples: Basics · Control flow · Bioinformatics","title":"Examples: complex DAGs"},{"category":"section","location":"examples/complex-dags/#3.1-Multi-stage-parallel","page":"Complex DAGs","text":"Flow: Two parallel fetch+transform branches, then merge, analyze, then report and archive in parallel.\n\n  ┌── fetch_db   ──►  transform_db  ──┐\n  │                                    ├──►  merge  ──►  analyze  ──►  ┌── report\n  └── fetch_files ──►  transform_files ─┘                               └── archive\n\nGoal: Runnable without network; shows layered & and >>.\n\nusing SimplePipelines\n\nfetch_db = @step db = sh\"echo '{\\\"db\\\":1}' > db_data.json\"\nfetch_files = @step files = sh\"echo 'local_data' > local_data.txt\"\n\ntransform_db = @step transform_db = sh\"wc -c db_data.json > db_size.txt\"\ntransform_files = @step transform_files = sh\"wc -c local_data.txt > files_size.txt\"\n\nmerge = @step merge = sh\"cat db_size.txt files_size.txt > merged.txt\"\nanalyze = @step analyze = sh\"wc -l merged.txt > results.txt\"\n\nreport = @step report = sh\"cat results.txt\"\narchive = @step archive = sh\"gzip -c merged.txt > results.tar.gz\"\n\ndb_branch = fetch_db >> transform_db\nfiles_branch = fetch_files >> transform_files\npipeline = (db_branch & files_branch) >> merge >> analyze >> (report & archive)\n\nrun(pipeline)","title":"3.1 Multi-stage parallel"},{"category":"section","location":"examples/complex-dags/#3.2-Robust-pipeline-(combined-features)","page":"Complex DAGs","text":"Flow: Retry+fallback fetch → conditional process → parallel report and notify (with retry on notify).\n\n  [primary^3 | backup]  ──►  [small? quick : full]  ──►  report\n                                                          └── notify^2\n\nGoal: Illustrate retries, fallback, branching, and parallel tail in one graph.\n\nusing SimplePipelines\n\nprimary_source = @step primary = sh\"echo '{\\\"status\\\":\\\"ok\\\"}' > data.json\"\nbackup_source = @step backup = sh\"echo '{\\\"status\\\":\\\"fallback\\\"}' > data.json\"\nfetch = Retry(primary_source, 3, delay=0.1) | backup_source\n\nquick_process = @step quick = sh\"wc -c data.json > output.txt\"\nfull_process = @step parse = sh\"wc -l data.json > output.txt\" >> @step validate = sh\"wc -c output.txt >> output.txt\"\n\nprocess = Branch(\n    () -> filesize(\"data.json\") < 1_000_000,\n    quick_process,\n    full_process,\n)\n\nreport = @step report = sh\"cat output.txt\"\nnotify = @step notify = sh\"echo 'Pipeline done'\"\n\npipeline = fetch >> process >> (report & Retry(notify, 2))\nrun(Pipeline(pipeline, name=\"Robust ETL\"))","title":"3.2 Robust pipeline (combined features)"},{"category":"section","location":"design/#Design","page":"Internals","text":"","title":"Design"},{"category":"section","location":"design/#Interface-Overview","page":"Internals","text":"┌─────────────────────────────────────────────────────────────┐\n│                    SimplePipelines.jl                       │\n├─────────────────────────────────────────────────────────────┤\n│                                                             │\n│  @step name = sh\"cmd\"       Shell step                      │\n│  @step name = sh\"cmd > f\"   Shell with redirection/pipes    │\n│  @step name = () -> ...     Julia step                      │\n│                                                             │\n│  a >> b                     Sequential; pass output to next (function step) │\n│  a & b                      Parallel: a and b together      │\n│  a |> b                     Pipe: run b with a's output(s)  │\n│  a >>> b                    Same input: both get same context │\n│  a .>> b                    Broadcast: attach b to each branch of a │\n│                                                             │\n│  run(p)                     Execute the pipeline            │\n│                                                             │\n└─────────────────────────────────────────────────────────────┘","title":"Interface Overview"},{"category":"section","location":"design/#Type-Hierarchy","page":"Internals","text":"AbstractNode\n    │\n    ├── Step{F}           Single unit of work\n    │                     F = Cmd | Function\n    │\n    ├── Sequence          Sequential execution\n    │                     children::Vector{AbstractNode}\n    │\n    └── Parallel          Concurrent execution\n                          children::Vector{AbstractNode}\n\nLeaf and wrapper nodes are parametric where it matters (Step{F}, Retry{N}, StepResult{S,I,O,V}), while wide child lists use Vector{AbstractNode} to keep large DAGs practical to compile.","title":"Type Hierarchy"},{"category":"section","location":"design/#Composition-Model","page":"Internals","text":"# User writes:\nsh\"echo a\" >> (sh\"echo b\" & sh\"echo c\") >> sh\"echo d\"\n\n# Becomes:\nSequence(AbstractNode[\n    Step{Cmd}(...),\n    Parallel(AbstractNode[Step{Cmd}(...), Step{Cmd}(...)]),\n    Step{Cmd}(...),\n])\n\nThe node kind still drives execution through dispatch. Child lists are stored as vectors so pipelines with hundreds or thousands of nodes do not create enormous tuple types.","title":"Composition Model"},{"category":"section","location":"design/#Execution-Flow","page":"Internals","text":"Execution is recursive: dispatch on node type and recurse.\n\nrun(Pipeline)\n       │\n       ▼\nrun_node(root, v, force)  ─── dispatch on node type\n       │\n       ├─► Step:     execute(step) → StepResult\n       ├─► Sequence: run_node each in order; break on first failure\n       ├─► Parallel: @spawn run_node each; fetch and concat\n       ├─► ForEach:  (String) find file matches, or (Vector) iterate items; get nodes from block (cycle check), then run like Parallel\n       └─► Retry/Fallback/Branch/Timeout/Force/Reduce: recurse on inner node(s)\n       │\n       ▼\nVector{AbstractStepResult}","title":"Execution Flow"},{"category":"section","location":"design/#Key-Design-Decisions","page":"Internals","text":"","title":"Key Design Decisions"},{"category":"section","location":"design/#1.-Vector-Backed-Child-Lists","page":"Internals","text":"# Used intentionally for scalable graph shape\nSequence(nodes::Vector{AbstractNode})\n\n# Leaf/runtime payloads remain parametric\nStep{Cmd}\nStepResult{S,I,O,V}\n\nThis trades a small amount of dynamic dispatch at graph traversal points for much lower compile-time and memory pressure on wide DAGs.","title":"1. Vector-Backed Child Lists"},{"category":"section","location":"design/#2.-Multiple-Dispatch,-Not-Type-Checks","page":"Internals","text":"# ✗ Runtime type checking (type unstable)\nfunction run_node(node)\n    if node isa Step\n        # ...\n    elseif node isa Sequence\n        # ...\n    end\nend\n\n# ✓ Multiple dispatch (type stable)\nrun_node(step::Step, v) = execute(step)\nrun_node(seq::Sequence, v) = _run_sequence!([], seq.nodes, v)\nrun_node(par::Parallel, v) = _spawn_parallel(par.nodes, v)","title":"2. Multiple Dispatch, Not Type Checks"},{"category":"section","location":"design/#3.-Bounded-Parallel-Traversal","page":"Internals","text":"tasks = [@spawn run_node(nodes[k], ctx, forced, contexts[k]) for k in rng]\nappend!(results, fetch(t))\n\nParallel and ForEach traversal runs in batches controlled by jobs, with optional resource hints from with_resources.","title":"3. Bounded Parallel Traversal"},{"category":"section","location":"design/#4.-Per-Run-Context","page":"Internals","text":"ctx = RunContext(verbose=verbose, jobs=jobs, state_path=state_path)\nrun_node(root, ctx, force, nothing)\n\nMutable run state, logging locks, resource budgets, spill policy, and memoization live in one RunContext, so concurrent run(...) calls do not share scheduler state.","title":"4. Per-Run Context"},{"category":"section","location":"design/#Performance-Characteristics","page":"Internals","text":"Aspect Design Choice Benefit\nNode storage Vector{AbstractNode} for children Scales to wide DAGs\nDispatch Multiple dispatch Avoids runtime type-check trees\nIteration Bounded loops + @spawn Predictable concurrency\nResults Parametric StepResult values in Vector{AbstractStepResult} Heterogeneous traces without Any fields\nState Per-run RunContext Isolates memoization and resource budgets","title":"Performance Characteristics"},{"category":"section","location":"reference/quickref/#Quick-reference","page":"Quick reference","text":"One-page lookup for syntax and APIs. Narrative explanations live in the user guide.","title":"Quick reference"},{"category":"section","location":"reference/quickref/#Steps","page":"Quick reference","text":"Syntax Meaning\n@step name = sh\"cmd\" Shell command\n@step name = sh(\"\\$(var)\") Shell with Julia interpolation\n@step name = sh(cmd_func) Run-time command, e.g. sh(() -> \"tool \" * ref)\nshell_raw\"...\", shell_raw\"\"\"...\"\"\" Literal shell (\\$VAR not expanded by Julia)\n@step name = () -> expr Julia function step","title":"Steps"},{"category":"section","location":"reference/quickref/#Rules-and-workflows","page":"Quick reference","text":"Syntax Meaning\n@rule name(\"in/{x}\" => \"out/{x}\") = \"cmd {input} > {output}\" Pattern rule\ncheck(rule) Inspect wildcard names and templates\ncheck(rule, \"out/A\") Test one concrete target\n@targets \"out/{x}\" x=[\"A\", \"B\"] Expand target pattern\n@workflow \"name\" begin ... end Collect rules and targets\nexplain(wf; target=\"out/A\") Explain rule dependency chain","title":"Rules and workflows"},{"category":"section","location":"reference/quickref/#Operators","page":"Quick reference","text":"Op Meaning Example\n>> Sequence; passes output into the next function step a >> b >> c\n& Parallel a & b & c\n| Fallback if primary fails a | b\n^n Retry up to n times a^3\n|> Pipe outputs into RHS a |> b\n>>> Same input to both sides a >>> b\n.>> Broadcast: attach RHS to each branch fe .>> step","title":"Operators"},{"category":"section","location":"reference/quickref/#Sequencing-and-piping","page":"Quick reference","text":"When the left side has multiple outputs (ForEach, Parallel), >>, |>, and .>> differ:\n\n| Left | >> | |> | .>> | |:––-|:––-|:––-|:–––| | Single | one value → next | one value | one value | | Multi | last only | vector of all | per branch |","title":"Sequencing and piping"},{"category":"section","location":"reference/quickref/#Control-flow-helpers","page":"Quick reference","text":"API Role\nTimeout(a, secs) Fail if overrun\nBranch(cond, a, b) Conditional subgraph (cond() or cond(upstream) after >>)\n@branch flag[] a b Sugar for Branch(() -> flag[], a, b)\nForEach(xs) do ... end Fan-out over collection\nForEach(\"pat/{x}.ext\") do ... end Fan-out from glob pattern\nReduce(f, a & b) Merge parallel outputs\nRetry(a, n; delay=d) Retries with optional delay","title":"Control-flow helpers"},{"category":"section","location":"reference/quickref/#Execution","page":"Quick reference","text":"Call / field Role\nrun(p) / p |> run Execute; returns result vector\nrun(..., jobs=N) Cap live parallel branches\nwith_resources(step; mem_mb=M, threads=T) Declare heavy-node resource use\nrun(..., verbose=false) Quiet (no command echo)\nrun(..., dry_run=true) Preview only\nrun(..., report=(res; pipeline) -> …) Hook after run, before saving state\n.success, .duration, .result, .inputs, .outputs Per-step outcome fields\nmaterialize_table(FilePath(p)) After using CSV, DataFrames: CSV → DataFrame","title":"Execution"},{"category":"section","location":"reference/quickref/#Utilities","page":"Quick reference","text":"API Role\nprint_dag(node) Print structure\ncount_steps(node) Step count\nsteps(node) Flat list of steps","title":"Utilities"},{"category":"section","location":"reference/quickref/#Full-detail","page":"Quick reference","text":"Narrative walkthroughs: User guide and Examples. Generated docstrings: API.","title":"Full detail"},{"category":"section","location":"guide/decision-guide/#Choosing-the-right-shape","page":"Choosing the right shape","text":"SimplePipelines gives you two complementary ways to build a DAG. You can write the graph directly with operators, or you can describe file patterns with rules. Both end up as the same kind of runnable pipeline.","title":"Choosing the right shape"},{"category":"section","location":"guide/decision-guide/#Start-With-The-Shape-Of-Your-Problem","page":"Choosing the right shape","text":"If you have a handful of concrete steps, write them directly:\n\nprepare >> sort_file >> (count_lines & count_bytes)\n\nThis is the clearest form when the filenames are fixed and the graph is small.\n\nIf you have repeated file patterns, use rules:\n\n@rule align(\"raw/{sample}.fq\" => \"out/{sample}.bam\") =\n    \"bwa mem ref.fa {input} > {output}\"\n\nRules are better when the interesting question is \"what target do I want?\", and the package should work backward to find dependencies.","title":"Start With The Shape Of Your Problem"},{"category":"section","location":"guide/decision-guide/#Use-Diagnostics-Before-Running","page":"Choosing the right shape","text":"For one rule, use check:\n\ncheck(align, \"out/A.bam\")\n\nFor a composed workflow, use explain:\n\nexplain(wf; target=\"out/A.bam\")\n\nThose commands are there so you can inspect wildcard values and dependency chains without executing shell commands.","title":"Use Diagnostics Before Running"},{"category":"section","location":"guide/decision-guide/#Choose-Operators-Only-When-Needed","page":"Choosing the right shape","text":"Most explicit DAGs start with >> and &. Add the value-passing variants only when a function step needs a specific branch result:\n\nuse |> when one function should receive all branch outputs;\nuse .>> when the next step should run once per branch;\nuse >>> when two steps inside a branch should receive the same original input.\n\nThe Composing pipelines page shows these in context. If you find yourself checking that table often, the graph may be clearer as a rule-based workflow or a ForEach.","title":"Choose Operators Only When Needed"},{"category":"section","location":"guide/decision-guide/#Resource-Hints-Are-Optional","page":"Choosing the right shape","text":"The default run is conservative: fan-out is bounded, large results spill to disk, and declared resource hints can limit concurrent memory or thread usage. You only need with_resources for steps that you know are heavy:\n\nheavy = with_resources(step; mem_mb=4_000, threads=4)\nrun(heavy & other)\n\nBy default, run already caps live parallel branches with jobs, caps declared memory at half of system RAM, and caps declared threads at the same host-aware limit as jobs. Pass explicit budgets only when you want stricter or looser limits.\n\nFor details on results and spill behavior, see Running and inspecting.","title":"Resource Hints Are Optional"},{"category":"section","location":"guide/running-and-results/#Running-and-inspecting","page":"Running and inspecting","text":"After a DAG looks right, run executes it and returns a vector of results. The printed output is for humans; the returned values are for code.","title":"Running and inspecting"},{"category":"section","location":"guide/running-and-results/#Run-A-Pipeline","page":"Running and inspecting","text":"results = run(pipeline)\n\nBy default, shell commands are printed before they run. Turn that off when the pipeline is already familiar:\n\nresults = run(pipeline; verbose=false)\n\nTo inspect structure without executing work, use print_dag or dry_run:\n\nprint_dag(pipeline)\nrun(pipeline; dry_run=true)\n\nIf you want a name in logs, wrap the DAG in a Pipeline:\n\np = Pipeline(pipeline; name=\"qc\")\nrun(p)","title":"Run A Pipeline"},{"category":"section","location":"guide/running-and-results/#Read-Results","page":"Running and inspecting","text":"Each entry is a StepResult. It tells you whether the step succeeded, how long it took, which files were declared, and what value the step returned.\n\nresults = run(pipeline; verbose=false)\n\nif all(r -> r.success, results)\n    println(\"all steps completed\")\nelse\n    failed = filter(r -> !r.success, results)\n    println(\"failed steps: \", join((r.step.name for r in failed), \", \"))\nend\n\nThe final result is often enough for simple pipelines:\n\nlast(results).result","title":"Read Results"},{"category":"section","location":"guide/running-and-results/#Report-After-A-Run","page":"Running and inspecting","text":"Use a report callback when another system needs a summary. It runs after the DAG finishes and before state is saved.\n\nrun(pipeline; report=(res; pipeline) -> begin\n    @info \"pipeline finished\" pipeline steps=length(res)\nend)","title":"Report After A Run"},{"category":"section","location":"guide/running-and-results/#Memory-Safety","page":"Running and inspecting","text":"SimplePipelines assumes disk is cheaper than RAM, and that unbounded parallelism can make a laptop unusable. A default run(pipeline) therefore has three safety limits:\n\njobs=default_jobs() runs at most min(Threads.nthreads(), 8) parallel branches at once.\nauto_spill=true spills large captured results to disk instead of keeping every result in RAM.\nmemory_budget_mb=default_memory_budget_mb() and thread_budget=default_thread_budget() limit nodes annotated with with_resources.\n\nMost users do not need to configure these for small pipelines.\n\nThe resource budgets apply when a step declares what it needs:\n\nheavy = with_resources(step; mem_mb=4_000, threads=4)\nrun(heavy & other)\n\nA node that declares both must fit both budgets before it starts. The caps are composed: memory pressure can queue the node, CPU-thread pressure can queue the node, and either one is enough to delay it until resources are free.\n\nUnannotated steps are still protected by the jobs branch cap, but the runtime cannot guess how much RAM or how many CPU threads an external tool will use. Add with_resources around known-heavy nodes so they queue instead of running all at once.","title":"Memory Safety"},{"category":"section","location":"guide/running-and-results/#Spilled-Results","page":"Running and inspecting","text":"When you do need to read a spilled value, call materialize:\n\nvalue = materialize(last(results).result)\n\nFor big data, the most predictable pattern is to write files yourself and return FilePath. Then downstream steps can load or stream that path as needed.\n\nwrite_table = @step write_table = () -> begin\n    # write result.csv here\n    FilePath(\"result.csv\")\nend\n\nTo make CSV loading convenient, load CSV/DataFrames and use materialize_table:\n\nusing CSV, DataFrames\ndf = materialize_table(FilePath(\"result.csv\"))\n\nYou can tighten or disable spilling for special cases:\n\nrun(pipeline; spill_threshold_bytes=1_000_000)\nrun(pipeline; auto_spill=false)","title":"Spilled Results"},{"category":"section","location":"guide/running-and-results/#Useful-Inspection-Helpers","page":"Running and inspecting","text":"count_steps(pipeline)\nsteps(pipeline)\nprint_dag(pipeline)\n\nUse these while developing a DAG; use check and explain for rule-based workflows.","title":"Useful Inspection Helpers"},{"category":"section","location":"guide/running-and-results/#What-To-Remember","page":"Running and inspecting","text":"Run returns data; printed logs are only a view. Keep big values on disk when you can, inspect the DAG before long runs, and read StepResult when automating.\n\nNext: Rules and diagnostics shows how to scale from one explicit DAG to many pattern-generated targets.\n\nFor compact syntax lookup, use the quick reference.","title":"What To Remember"},{"category":"section","location":"guide/steps-and-shell/#Steps-and-shell","page":"Steps and shell","text":"Everything starts with a step. A step is one piece of work: either a shell command or a Julia function. Later pages show how to connect steps, but first it helps to be clear about what a step owns.\n\nA step has:\n\na name, used in output and state;\nwork, such as sh\"sort data.txt\" or () -> compute();\noptional input and output file paths.\n\nThe file paths are not just documentation. They let SimplePipelines check that inputs exist, verify outputs after a command runs, and skip fresh work on later runs.","title":"Steps and shell"},{"category":"section","location":"guide/steps-and-shell/#Shell-commands","page":"Steps and shell","text":"Use sh\"...\" when the command is known while constructing the pipeline.\n\nusing SimplePipelines\n\nsort_file = @step sort_file = sh\"sort data.txt > sorted.txt\"\n\nShell syntax such as redirects and pipes belongs inside the string:\n\nunique_lines = @step unique_lines =\n    sh\"sort data.txt | uniq > unique.txt\"\n\nIf the command depends on a Julia value, build it with sh(...):\n\nsample = \"A\"\nalign = @step align = sh(\"bwa mem ref.fa reads_$(sample).fq > $(sample).sam\")\n\nFor commands that should be generated at run time, pass a function to sh:\n\nsample_ref = Ref(\"A\")\nalign = @step align = sh(() -> \"bwa mem ref.fa reads_$(sample_ref[]).fq > out.sam\")\n\nUse shell_raw\"...\" when the shell script itself contains $VAR and Julia should not try to interpolate it.\n\nRule work looks different on purpose:\n\n@rule align(\"raw/{sample}.fq\" => \"out/{sample}.bam\") =\n    \"bwa mem ref.fa {input} > {output}\"\n\nThat string is a rule template, not a Cmd. It is rendered later, when a concrete target provides wildcard values. Rules are covered in Rules and diagnostics.","title":"Shell commands"},{"category":"section","location":"guide/steps-and-shell/#Julia-functions","page":"Steps and shell","text":"Function steps are useful for small transformations, summaries, or glue logic that is clearer in Julia than in shell.\n\nmake_report = @step report = () -> begin\n    n = countlines(\"sorted.txt\")\n    \"sorted.txt has $n lines\"\nend\n\nWhen a function step follows another step with >> or |>, it can receive the previous result. The next page explains those operators in context.\n\nemit_path = @step emit_path = () -> \"data.txt\"\nread_file = @step read_file = path -> read(path, String)\n\npipeline = emit_path >> read_file","title":"Julia functions"},{"category":"section","location":"guide/steps-and-shell/#File-dependencies-(optional)","page":"Steps and shell","text":"A step that writes files should usually declare them:\n\nalign = @step align(\"reads.fq\" => \"aligned.bam\") =\n    sh\"bwa mem ref.fa reads.fq > aligned.bam\"\n\nThis reads as: \"align needs reads.fq and promises aligned.bam.\"\n\nInputs and outputs can be vectors when there are several files:\n\nmerge = @step merge([\"a.bam\", \"b.bam\"] => [\"merged.bam\"]) =\n    sh\"samtools merge merged.bam a.bam b.bam\"","title":"File dependencies (optional)"},{"category":"section","location":"guide/steps-and-shell/#What-To-Remember","page":"Steps and shell","text":"Write the smallest honest step. If a command creates a file, declare that file. If a short piece of logic is easier in Julia, make it a function step. Once each unit is clear, the DAG becomes easy to read.\n\nNext: Composing pipelines shows how >> and & turn these steps into a workflow.","title":"What To Remember"},{"category":"section","location":"reference/public-api/#Public-API-policy","page":"Public API policy","text":"This page clarifies what is considered stable for semver (minor releases may add features; patch releases are bugfixes only) versus internal details that may change without notice.","title":"Public API policy"},{"category":"section","location":"reference/public-api/#Stable-(documented-exported)","page":"Public API policy","text":"Treat the following as the supported surface:\n\nEverything listed under export in the package module (see the API page and src/SimplePipelines.jl).\nDocumented keyword arguments of run (including report, resource and spill options).\nStepFailure kind symbols documented for user-visible outcomes (e.g. :process_failed, :exception, :timed_out, :inner_exception, :no_matches, …).\n\nThe persisted state file uses a fixed binary layout (magic SPstate\\\\0); the on-disk format is considered stable within the same major version unless called out in the changelog.","title":"Stable (documented + exported)"},{"category":"section","location":"reference/public-api/#Extension-hooks","page":"Public API policy","text":"materialize / materialize_table: specialise materialize on your own types in user code; materialize_table for FilePath is provided when the Table I/O package extension loads (using CSV, DataFrames after SimplePipelines).","title":"Extension hooks"},{"category":"section","location":"reference/public-api/#Internal-(do-not-rely-on)","page":"Public API policy","text":"Submodules such as StateFormat (implementation detail for persistence).\nFunction names not exported (e.g. layout helpers, internal run_node helpers).\nExact log text or styling from verbose mode.\n\nIf you need a symbol that is not exported, open an issue: it may be a candidate for promotion to the public API.","title":"Internal (do not rely on)"},{"category":"section","location":"examples/control-flow/#Examples:-control-flow","page":"Control flow","text":"Cookbook examples for retry, fallback, and conditional branches. Read Control flow first if you want the concepts before the recipes.\n\nMore examples: Basics · Complex DAGs · Bioinformatics","title":"Examples: control flow"},{"category":"section","location":"examples/control-flow/#2.1-Retry-a-flaky-step","page":"Control flow","text":"Flow: Run a step up to N times, then continue.\n\n  [fetch^3]  ──►  process\n\nGoal: Retry a fetch before processing its output.\n\nusing SimplePipelines\n\nfetch = @step fetch = sh\"echo '{\\\"x\\\":1}' > data.json\"\nprocess = @step process = sh\"wc -c data.json\"\npipeline = Retry(fetch, 3, delay=0.1) >> process\nrun(pipeline)","title":"2.1 Retry a flaky step"},{"category":"section","location":"examples/control-flow/#2.2-Fallback-to-a-backup-method","page":"Control flow","text":"Flow: If the primary method fails, run the backup.\n\n   primary  ──►  (success) result\n      │\n      └──►  fallback ──►  result\n\nGoal: Produce sorted.csv even when the fast path fails.\n\nusing SimplePipelines\n\nrun(@step setup = sh\"(echo 'a,b'; echo '1,2') > data.csv\", verbose=false)\nfast = @step fast = sh\"false\"\nslow = @step slow = sh\"cat data.csv > sorted.csv\"\npipeline = fast | slow\nrun(pipeline)","title":"2.2 Fallback to a backup method"},{"category":"section","location":"examples/control-flow/#2.3-Retry,-then-fallback","page":"Control flow","text":"Goal: Retry the primary method, then use the backup if every attempt fails.\n\nusing SimplePipelines\n\nrun(@step setup = sh\"(echo 'a,b'; echo '1,2') > data.csv\", verbose=false)\nfast = @step fast = sh\"false\"\nslow = @step slow = sh\"cat data.csv > sorted.csv\"\npipeline = Retry(fast, 3) | slow\nrun(pipeline)","title":"2.3 Retry, then fallback"},{"category":"section","location":"examples/control-flow/#2.4-Conditional-branching","page":"Control flow","text":"Flow: One of two branches runs based on a condition.\n\n            ┌── if true  ──►  branch_a\n  condition ─┤\n            └── if false ──►  branch_b\n\nGoal: Choose processing by file size or environment.\n\nusing SimplePipelines\n\nrun(@step setup = sh\"(echo 'a,b'; echo '1,2'; echo '3,4') > data.csv\", verbose=false)\n\nsmall_pipeline = @step small = sh\"head -n 1000 data.csv > sample.csv\"\nlarge_pipeline = @step decompress = sh\"gunzip -c data.csv.gz > data.csv\" >> @step process = sh\"split -l 10000 data.csv chunk_\"\npipeline = Branch(\n    () -> filesize(\"data.csv\") < 100_000_000,\n    small_pipeline,\n    large_pipeline,\n)\nrun(pipeline)\n\ndebug_steps = @step debug = sh\"echo 'debug mode'\"\nprod_steps = @step prod = sh\"echo 'production'\"\npipeline = Branch(() -> get(ENV, \"DEBUG\", \"0\") == \"1\", debug_steps, prod_steps)\nrun(pipeline)","title":"2.4 Conditional branching"},{"category":"section","location":"guide/foreach-reduce/#Fan-out-and-reduce","page":"Fan-out and reduce","text":"Use ForEach when the same subgraph should run many times. Use Reduce when parallel branch outputs need to become one value.\n\nThis is different from rules: rules start from target file names and resolve dependencies. ForEach starts from a collection or discovered files and builds a branch for each item.","title":"Fan-out and reduce"},{"category":"section","location":"guide/foreach-reduce/#Fan-Out-Over-A-Collection","page":"Fan-out and reduce","text":"The simplest ForEach is like map, but each item becomes a pipeline branch.\n\nsamples = [\"sample_A\", \"sample_B\", \"sample_C\"]\n\npipeline = ForEach(samples) do s\n    @step process = sh(\"analyze $(s).fastq > $(s).txt\")\nend\n\nThe block returns a step or node. It is evaluated when the pipeline runs, so the fan-out can reflect the current collection or filesystem.","title":"Fan Out Over A Collection"},{"category":"section","location":"guide/foreach-reduce/#Discover-Existing-Files","page":"Fan-out and reduce","text":"Pattern mode scans the filesystem and captures wildcard values from matching files.\n\ncd(mktempdir()) do\n    mkpath(\"fastq\")\n    write(\"fastq/A_R1.fq.gz\", \"\")\n    write(\"fastq/B_R1.fq.gz\", \"\")\n\n    pipeline = ForEach(\"fastq/{sample}_R1.fq.gz\") do sample\n        @step align = sh(\"align fastq/$(sample)_R1.fq.gz > $(sample).bam\")\n    end\n\n    run(pipeline)\nend\n\nMultiple wildcards become multiple arguments:\n\nForEach(\"data/{project}/{sample}.csv\") do project, sample\n    @step process = sh(\"process data/$(project)/$(sample).csv\")\nend\n\nUse pattern mode when the input files already exist and should drive the work. Use rules when the requested output target should drive dependency resolution.","title":"Discover Existing Files"},{"category":"section","location":"guide/foreach-reduce/#Resource-Limits","page":"Fan-out and reduce","text":"Fan-out can create many runnable branches. By default, run limits live branches with jobs=default_jobs(), so a large ForEach queues work instead of starting everything at once.\n\nIf each branch calls a heavy tool, declare that too:\n\npipeline = ForEach(samples) do sample\n    step = @step align = sh(\"bwa mem -t 4 ref.fa $(sample).fq > $(sample).bam\")\n    with_resources(step; mem_mb=8_000, threads=4)\nend\n\nrun(pipeline)\n\nThe default memory and thread budgets then keep declared-heavy branches from running together when they would exceed the host-aware caps.","title":"Resource Limits"},{"category":"section","location":"guide/foreach-reduce/#Reduce-Branch-Outputs","page":"Fan-out and reduce","text":"Reduce runs a node and gives successful branch results to one reducer function.\n\nanalyze_a = @step a = sh\"echo result_a\"\nanalyze_b = @step b = sh\"echo result_b\"\n\npipeline = Reduce(analyze_a & analyze_b) do outputs\n    join(outputs, \"\\n\")\nend\n\nThe reducer receives a vector. The element type depends on upstream step results, so keep reducers small and explicit.","title":"Reduce Branch Outputs"},{"category":"section","location":"guide/foreach-reduce/#Large-Data","page":"Fan-out and reduce","text":"For large branch results, do not return big objects from every branch if the reducer can stream files instead. Return paths:\n\nbranches = ForEach([\"A\", \"B\"]) do sample\n    @step write_result = () -> begin\n        path = \"$(sample).txt\"\n        write(path, \"result for $sample\")\n        FilePath(path)\n    end\nend\n\nThen the reducer can open each path one at a time.","title":"Large Data"},{"category":"section","location":"guide/foreach-reduce/#What-To-Remember","page":"Fan-out and reduce","text":"ForEach repeats a branch. Reduce combines branch results. If the work is file-target driven, consider rules and @workflow first; if it is item-driven, ForEach is usually clearer.\n\nNext: Running and inspecting explains execution, results, and memory behavior.","title":"What To Remember"},{"category":"section","location":"examples/basics/#Examples:-basics","page":"Basics","text":"Runnable recipes for the core DAG operators. These examples assume you have read Getting started, or already know @step, >>, &, and run.\n\nMore examples: Control flow · Complex DAGs · Bioinformatics","title":"Examples: basics"},{"category":"section","location":"examples/basics/#1.1-Basic-pipeline","page":"Basics","text":"Flow: Three steps in sequence.\n\n  download  ──►  process  ──►  upload\n\nGoal: Create a file, process it, then copy the result (runnable without network).\n\nusing SimplePipelines\n\ndownload = @step download = sh\"(echo line3; echo line1; echo line2) > data.txt\"\nprocess = @step process = sh\"sort data.txt > sorted.txt\"\nupload = @step upload = sh\"cp sorted.txt uploaded.txt\"\n\npipeline = download >> process >> upload\nrun(pipeline)","title":"1.1 Basic pipeline"},{"category":"section","location":"examples/basics/#1.2-Parallel-processing","page":"Basics","text":"Flow: Three steps run in parallel, then one step merges.\n\n       ┌── file_a ──┐\n       ├── file_b ──┼──►  archive\n       └── file_c ──┘\n\nGoal: Create three files, compress them concurrently, then archive.\n\nusing SimplePipelines\n\nfile_a = @step a = sh\"echo content_a > file_a.txt && gzip -k file_a.txt\"\nfile_b = @step b = sh\"echo content_b > file_b.txt && gzip -k file_b.txt\"\nfile_c = @step c = sh\"echo content_c > file_c.txt && gzip -k file_c.txt\"\narchive = @step archive = sh\"tar -cvf archive.tar file_a.txt.gz file_b.txt.gz file_c.txt.gz\"\n\npipeline = (file_a & file_b & file_c) >> archive\nrun(pipeline)","title":"1.2 Parallel processing"},{"category":"section","location":"examples/basics/#1.3-Julia-shell","page":"Basics","text":"Flow: Julia step → shell step → Julia step.\n\n  generate  ──►  process  ──►  report\n   (Julia)       (shell)      (Julia)\n\nGoal: Generate data in Julia, run a shell tool, then summarize in Julia.\n\nusing SimplePipelines\nusing DelimitedFiles\n\ngenerate = @step generate = () -> begin\n    data = rand(100, 10)\n    writedlm(\"matrix.csv\", data, ',')\n    return \"Generated $(size(data)) matrix\"\nend\n\nprocess = @step process = sh\"wc -l matrix.csv\"\n\nreport = @step report = () -> begin\n    lines = read(\"matrix.csv\", String)\n    nrows = count(==('\\n'), lines)\n    return \"Matrix has $nrows rows\"\nend\n\npipeline = generate >> process >> report\nrun(pipeline)","title":"1.3 Julia + shell"},{"category":"section","location":"examples/basics/#1.4-Data-passing-and-pipe","page":"Basics","text":"Flow: Function steps receive the previous step's output; |> pipes output forward; .>> attaches the next step to each branch.\n\n  download(id)  ──►  process(path)     # >> passes path\n  ForEach .>> step                     # each branch: branch >> step\n\nGoal: Pass output between function steps; use broadcast for per-branch follow-up.\n\nusing SimplePipelines\n\ndownload() = \"data.csv\"\nprocess(path) = path * \"_done\"\npipeline = @step dl = download >> @step proc = process\nrun(pipeline)\n\nfetch = @step fetch = `echo \"hello\"`\npipeline = fetch |> @step process = (s -> \"got: \" * strip(String(s)))\nrun(pipeline)\n\nfe = ForEach([1, 2]) do x\n    Step(Symbol(\"gen_\", x), `echo $x`)\nend\npipeline = fe .>> @step process = (s -> \"got_\" * strip(String(s)))\nrun(pipeline, force=true)","title":"1.4 Data passing and pipe"},{"category":"section","location":"guide/composition/#Composing-pipelines","page":"Composing pipelines","text":"Once steps are clear, composition should read like the workflow diagram you have in mind. SimplePipelines uses a few operators for this. Most pipelines only need two at first:\n\n>> for \"then\";\n& for \"at the same time\".\n\nThe other operators are for data-flow details once your graph has branches.","title":"Composing pipelines"},{"category":"section","location":"guide/composition/#Run-One-Step-After-Another","page":"Composing pipelines","text":"Use >> when a later step should wait for an earlier step.\n\nprepare = @step prepare([] => [\"data.txt\"]) =\n    sh\"(echo line3; echo line1; echo line2) > data.txt\"\n\nsort_file = @step sort_file([\"data.txt\"] => [\"sorted.txt\"]) =\n    sh\"sort data.txt > sorted.txt\"\n\npipeline = prepare >> sort_file\nrun(pipeline)\n\nThis shape is useful even when the shell commands already mention file names, because the DAG now knows ordering, freshness, and failure boundaries.","title":"Run One Step After Another"},{"category":"section","location":"guide/composition/#Fork,-Then-Join","page":"Composing pipelines","text":"Use & when independent branches can run concurrently.\n\nprepare -> sort_file -> count_lines\n                    \\-> count_bytes\n\ncount_lines = @step count_lines([\"sorted.txt\"] => [\"lines.txt\"]) =\n    sh\"wc -l sorted.txt > lines.txt\"\n\ncount_bytes = @step count_bytes([\"sorted.txt\"] => [\"bytes.txt\"]) =\n    sh\"wc -c sorted.txt > bytes.txt\"\n\npipeline = prepare >> sort_file >> (count_lines & count_bytes)\n\nParentheses matter: (a & b) >> c means both a and b must finish before c runs.","title":"Fork, Then Join"},{"category":"section","location":"guide/composition/#Passing-Values,-Not-Just-Files","page":"Composing pipelines","text":"File paths are often enough. When you use function steps, step results can also flow directly into the next function.\n\nemit_path = @step emit_path = () -> \"sorted.txt\"\nread_text = @step read_text = path -> read(path, String)\n\npipeline = emit_path >> read_text\n\nFor a single upstream result, >> and |> both pass that value to a function step. The difference matters when the left side has many branch results.","title":"Passing Values, Not Just Files"},{"category":"section","location":"guide/composition/#Advanced:-Many-Branch-Outputs","page":"Composing pipelines","text":"When a parallel node produces multiple results, choose the operator based on what the next function step should receive:\n\na >> b: pass the last branch result to b;\na |> b: pass a vector of all branch outputs to one function step;\na .>> b: run b once per branch;\na >>> b: run the second step with the same input as the first.\n\nleft = (@step a = `echo A`) & (@step b = `echo B`)\n\ncollect_all = @step collect_all = xs -> join(strip.(String.(xs)), \",\")\nper_branch = @step per_branch = x -> \"saw \" * strip(String(x))\n\npipeline_a = left |> collect_all\npipeline_b = left .>> per_branch\n\nUse these only when you need this value-passing behavior. For file-oriented workflows, plain >> and & usually keep the graph easier to read. For repeated branches over samples or files, continue to Fan-out and reduce.","title":"Advanced: Many Branch Outputs"},{"category":"section","location":"guide/composition/#What-To-Remember","page":"Composing pipelines","text":"Write the DAG the way you would draw it. Start with >> and &; reach for the pipe variants only when a function step needs a particular value from branch outputs.\n\nNext: Rules and diagnostics shows how to describe many file targets from patterns and inspect the result before running.","title":"What To Remember"},{"category":"section","location":"#SimplePipelines.jl","page":"Home","text":"Minimal, type-stable DAG pipelines for Julia\n\nSimplePipelines is for small-to-medium workflows where the shape of the work should be visible in Julia code. A step can be a shell command or a Julia function. Operators connect steps into a DAG. run executes that DAG and returns one result per step.\n\nThe central idea is deliberately small:\n\nstep_a >> step_b        # run in order\nstep_a & step_b         # run branches in parallel\nrun(pipeline)           # execute and collect StepResult values\n\nFor a complete first example, start with Getting started.","title":"SimplePipelines.jl"},{"category":"section","location":"#Learning-Paths","page":"Home","text":"If you are new, read the first path in order. If you already know the basics, jump to the shape that matches your workflow.","title":"Learning Paths"},{"category":"section","location":"#First-Pipeline","page":"Home","text":"Getting started -> Steps and shell -> Composing pipelines -> Running and inspecting\n\nThis path teaches one concrete DAG before introducing advanced features.","title":"First Pipeline"},{"category":"section","location":"#Pattern/Rule-Workflow","page":"Home","text":"Rules and diagnostics is the entry point when your files look like raw/{sample}.fq -> out/{sample}.bam. It starts with check(rule) so you can see wildcard values before running anything.","title":"Pattern/Rule Workflow"},{"category":"section","location":"#Cookbook-And-Reference","page":"Home","text":"Use Examples for runnable recipes, and the quick reference for compact syntax lookup.\n\nFull generated docstrings live on the API page.","title":"Cookbook And Reference"}]
}
